# Expired file cache disk reclamation

Implement and test locally. The user subsequently approved a local commit and deployment preparation. This unit runs local release checks and commits only. The manager must obtain exact push confirmation and handle deployment. Do not push or change production in this unit.

Only the configured default Laravel FileStore is eligible. Read bounded expiration/type headers, never deserialize or call cache get. Preserve live, forever, malformed-header, scalar, symlink, and locked entries. Apply requires --apply. Under a nonblocking exclusive file lock, check path/device/inode and expiration again, then truncate expired array/object files without unlinking. Retain zero-byte markers and directories for waiting writers.

Stream standard hash directories with a runtime bound and time-based shard rotation. Schedule local per-instance cleanup. Report aggregate counts/bytes and safe failures. Add focused tests and context.
