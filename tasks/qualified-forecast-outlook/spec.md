# Qualified forecast outlook

Implement the approved public rise/fall/approximately unchanged outlook using saved direction categories. Keep the repaired local v3 EWMA model, alpha 0.25, lambda 0.30 and threshold 0.15 unchanged. Use actual stored horizons and dates. Explain uncertainty and history coverage. Unknown and partial data must not imply stable prices. Preserve the layout and Preferred Sources.

Record four direction outcomes for new evaluations. Add a read-only median forecast report with strict saved-provenance compatibility groups, sample and issue-day counts, named skips and an unchanged-direction baseline. Preserve all completed actuals and old forecasts. No backfill, new schedule, dependency, migration, feature flag, model experiment, production operation, commit or push.

Verify focused tests, the full suite, production asset build, Pint, PHP syntax and diff checks. Update feature context. Release and initial v3 generation remain subject to explicit approval.
