# Decisions

- Use only export-20260913T094138Z. Validate manifest, every data/SQL hash, bytes and counts before measurement.
- Strict same-ID, same-basis General snapshots are the main daily panel. Snapshot IDs are not replacement lineages. Separate observed and canonical regimes; never count the seam as a price change.
- Unknown premium VAT does not exclude cadence evidence on a consistent scale. Do not infer premium levels.
- Stored lineage mappings require dated carrier provenance; active-tip template attributes do not prove historical terms or dated replacement decisions.
- No live extraction was needed: current active flags cannot reconstruct historical offer availability or decision dates.
- Verified all seven export payloads and SQL/count hashes before calculation. Main daily cohort exactly matches the prior supplier pilot's 11,608 eligible General rows. Unknown VAT remains allowed for same-scale numeric cadence; no premium is filled.
- Exact local IDs are not price hashes: the importer preserves IDs for known upstream API identities and creates random-prefix IDs for new API identities. There is only one same-ID energy change in 10,757 adjacent pairs, but 700 IDs first appear after study start. Do not interpret stable IDs as stable supplier offers.
- Restrict the lineage sensitivity to dated period carriers and matching numeric prices. Never apply the full stored replacement DAG retrospectively. Accepted mapping: 4,648 daily rows; 222 ambiguous; 6,738 unmapped. All 112 adjacent mapped energy changes cross disjoint carrier IDs. Seller launch intent and direct dated replacement edges remain unverified.
- Keep every observed constant fragment with left/right censor flags. Report complete change-to-change intervals only as a selected descriptive subset (82 lineage intervals, median 13.5 days); the same-ID panel has zero complete intervals. No unsupported population survival estimate is fitted.
- Supplier medians use exactly the same eligible General rows and retain campaign/variant mix. Of 473 adjacent median changes, 472 have a changed ID set, including 430 fully disjoint sets. Offered medians are not identical-product prices.
- Thirty-day canonical supplier endpoint pairs reproduce the prior pilot's 891 available pairs: 659 change and 232 remain equal. This weakens a general slow-repricing explanation but says nothing about futures prediction accuracy. No canonical 60/90-day pairs exist in the export.
- `analyze.py` and the independent `verify.py` passed. The latter reconstructs raw daily prices/medians, validates dated numeric carrier mapping, recomputes every exact window and verifies censor boundaries and denominators.
- All findings and output are confined to this new task. No application, existing task, production, database, forecast coefficient or release state was changed; no commit or push was made.

## Horizon and lag continuation

- The user extended this same task to the fixed 14/30/45/60-day tests and fixed 0/7/14/21/30/45/60-day descriptive change lags. Use only the same verified export; preserve the cadence artifacts. No application change, production access, database replacement, dependency, commit or push is permitted.
- Read the actual v3 forecast and hedge services and the earlier local PHP replay. Reproduce alpha .25, lambda .30, minimum 10, strict prior dates, next-full-month day-weighted FI Base strip, month/quarter/year fallback and VAT 1.255. Non-30-day fixed-gap results are sensitivity only. Do not fit the optional coefficient grid in this limited fixed-baseline experiment; no target-dependent parameter selection occurs.
- Forecast targets and the predeclared 14-day momentum input must each have exact same-basis unit evidence. Main model comparison is the three-model intersection within each horizon. Keep a gap-only supplemental cohort and the old 48-row/16-day replay cohort separate so the momentum history restriction does not silently change previous reported means.
- Canonical exact pairs total 105/57/12/0 rows by horizon; three-model pairs total 63/15/0/0. The canonical all-model 14/30/45 intersection is empty. Observed all-model pairs total 246/198/153/108; the 14/30/45 common-issue intersection has 153 rows per horizon. No cross-basis fill is allowed.
- Canonical all-model gap improvement relative to unchanged is -13.1% at 14 days and -5.8% at 30 days. Momentum is worse by 110.5% and 44.3%. These different target cohorts do not identify a best horizon. The old 30-day cohort reproduces v3 MAE .7458104167 and unchanged .70029375 exactly.
- Lag retail differences require eight complete calendar dates in one basis. The public market term series includes eligible Time/Season offers; the separate supplier series is the raw-snapshot General median panel from the earlier pilot. Five distinct adjacent daily change dates is the predeclared reporting guard; it is not a significance claim. Sign agreement excludes zero changes and retains its denominator.
- Primary lag evidence removes rolling-H windows whose delivery basket changes. Check all eight calendar basket starts and verify endpoint agreement; retain unscreened results and removed counts. No future-data roll correction is used. Fix common-date checks to the full grid and a secondary 0/7/14-day grid, not a data-selected winning subset.
- Full-grid roll-screened market intersections contain only one date per basis/term; supplier intersections contain zero or one. Correlation is unavailable there. Individual screened canonical 6-month correlations change sharply versus unscreened results (7-day .559 to -.118; 21-day -.059 to .799). This prevents a stable universal lag conclusion. Supplier cells and repeated windows are not independent futures observations.
- Independent Decimal/daily-expanded hedge and closed-form EWMA verification passed for 678 hedge rows, 1,047 forecast rows and 47,115 lag-change rows. The actual PHP services also passed all 678 hedge and 1,047 forecast checks in an isolated, proved in-memory SQLite fixture. The original database was not opened or replaced. Synthetic invalid/missing/date guards passed. A float half-tie in the untuned momentum baseline was corrected to explicit decimal half-up rounding during verification.
- Results are revised export-time evidence, not original archived vintages. No winning-horizon, causal supplier response, calibrated lag or production-release claim is made. Next test: a longer archived-vintage canonical forward panel with frozen baselines, exact outcomes, common issue cohorts and multiple distinct shocks/month rolls.
