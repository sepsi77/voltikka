# Stale fixed-contract forecast futures

Investigate why the production fixed-term contract price forecast still uses EEX futures dated 2026-07-31 instead of newer available futures. Identify the root cause and make only a necessary, safe correction if code or configuration is at fault.
