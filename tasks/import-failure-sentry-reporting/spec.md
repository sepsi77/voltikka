# Import failure Sentry reporting

Add one explicit Sentry Issue and one safe aggregate Laravel log per failed spot, EEX, or contract import invocation. Group repeated failures by import. Keep retries, pricing, partial-import safeguards, and existing exits. Do not report each request, maturity, interpretation, or optional logo failure. No production operations or new dependencies/configuration.

Verify real Sentry capture, severity, grouping, safe context and scope isolation, plus command failure and recovery paths.
