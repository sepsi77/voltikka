# Production rollout state

## Completed release
- Commit: `b47eea569be5da19d2d0493ccd571d969fbc7972`, pushed with approved `git push origin main`.
- Deployment: `7082d71a-8c52-4abd-8c44-199005778b59`, exact commit matched with Railway MCP; repository poll script reported SUCCESS.
- Final release tests: 2,276 passed / 10,522 assertions, 91.38 seconds. Vite build passed. Logs: `/tmp/annual-pricing-release-tests.log`, `/tmp/annual-pricing-release-build.log`.
- Production About and statistics pages returned HTTP 200. New plain-language methodology and `#menetelma` were verified. Production generated CSS `app-Xoj990xo.css`, different from the local CSS hash; its manifest reference and HTTP asset were verified. JS `app-Pr_ebGQi.js` matches the local build. The local CSS-hash assertion failed and was replaced by verification against the actual production manifest; the deployment itself succeeded.

## Read-only production inspection
At Helsinki `2026-09-11T21:45:11+03:00`:
- Deployed commit matches the release; canonical pricing enabled; active public annual method `annual_cost_as_of_v1`.
- V1 annual rows: 196,256; v1 annual aggregates: 6,639. Both cover 2026-01-21 through 2026-09-11.
- Legacy annual aggregates: 7,710. Snapshots: 75,413; source components: 239,811.
- No v2 annual rows existed yet. Current collection will write v2 on its next run.
- Inspection used explicit Railway IDs, an asserted MySQL connection, a read-only repeatable-read transaction, array cache, and rollback. No secret values were printed.
- Evidence: `/tmp/annual-v2-production-inspect.php` and `.log`.

## First historical preview (complete; candidate not approved)
- Historical command permits past dates only. The reviewed read-only range is 2026-01-21 through 2026-09-10, 232 available evidence dates. The current 2026-09-11 date is deliberately outside it. Do not move the clock or bypass that guard.
- First full-range attempt hit its explicit remote 1,200-second time limit (exit 124). It completed 79 dates through 2026-04-10. The log ends with that date's median summary; no calculation error was printed. This was an incomplete preview, not a successful complete run, and it made no writes.
- First log: `/tmp/annual-v2-production-preview.log`; PHP script: `/tmp/annual-v2-production-preview.php`.
- Remaining dates were run sequentially in bounded batches: April 11–30, May, June, July, August, September 1–10. Each used a fresh read-only transaction and a 1,200-second remote timeout. No parallel calculation processes were used. All six batches completed.
- Background job `job-91747-101` (`annual-v2-remaining-readonly-batches`) completed all six remaining batches successfully in 34m10s. Runner: `/tmp/annual-v2-preview-remaining.py`. Each batch writes `/tmp/annual-v2-production-preview-<FROM>-<TO>.log`. Combined coverage is all 232 available dates, with no duplicates; February 12 has no evidence.
- All preview sessions assert the exact deployed SHA, v1 active method, canonical mode, and MySQL read-only setting. They call the existing command without `--apply`. No historical apply or public activation has run.
- CLI telemetry session: `voltikka-annual-pricing-release-20260912-1`, caller `skill:use-railway@1.2.2`.

## Review findings and required next steps
- See `preview-b47eea5.md` for full counts, bounded outlier/loss review, and source logs. No v2 rows have been written; public v1 remains active.
- The outlier review confirmed a quarterly seasonal-reference mismatch: the last month's index represents a whole-quarter seller price. Prepare a narrow day-weighted quarter reference correction, keep monthly behavior and beta unchanged, and increment the deployed calculation-cache schema to 17. The user was told historical apply is paused while the correction is prepared.
- Implementation agent `1353a134-69c8-432` completed the narrow correction and `seasonal-reference-fix.md`. Parent reviewed the actual helper and regressions. Full suite: 2,283 passed / 10,830 assertions (91.16 seconds); production asset build and `git diff --check` passed. Logs: `/tmp/annual-v2-quarter-reference-tests.log` and `/tmp/annual-v2-quarter-reference-build.log`.
- A read-only local check gives a revised June 1 quarterly/5,000-kWh median of €724.74, versus €1,034.85 in the first production candidate and €469.70 in stored v1. Local and production results are not interchangeable; the corrected production range still needs a fresh preview.
- The user approved the second code release: commit and push `main` with `git push origin main` to Railway Voltikka / production / voltikka, deploying the quarter-reference fix and cache schema 17. This keeps public v1 active and does not authorize a historical apply or method switch. A fresh complete production preview follows successful deployment.
- After release approval and successful deployment, rerun a complete preview. Old output cannot serve as the corrected apply manifest. Re-check the real Helsinki date, latest v1/v2 dates, and current coverage rather than assuming September 11 is still today.

## Corrected release and fresh preview
- Approved correction committed and pushed as `f6518498c2b7598c87bd79e62855c18afba63c16` with `git push origin main`.
- Railway deployment `67088a70-dcb1-40a8-af30-aad87cab2c3e` matched that exact commit and reached SUCCESS through the repository poll script. Production statistics page returned HTTP 200.
- Read-only inspection at Helsinki `2026-09-12T06:00:39+03:00` verified SHA f651849, cache schema 17, canonical enabled, public v1, unchanged v1 counts (196,256 annual rows / 6,639 aggregates), and no v2 annual rows yet. Log: `/tmp/annual-v2-f651849-inspect.log`.
- September 11 is now a past date. A fresh full preview covers January 21 through September 11 in nine sequential bounded batches. Do not reuse the old b47eea5 results as corrected results.
- Completed job: `job-91747-119` (`annual-v2-corrected-production-preview`), all nine batches successful in 54m50s. Runner: `/tmp/annual-v2-corrected-preview-batches.py`. PHP wrapper: `/tmp/annual-v2-reviewed-preview.php`. Logs: `/tmp/annual-v2-f651849-preview-<FROM>-<TO>.log`.
- Each remote process asserts exact SHA f651849, schema17, public v1, mode c1r1, historical dates, MySQL read-only repeatable-read transaction, and uses array cache. Each has a 1,200-second remote timeout and 256 MiB memory limit. A failed batch stops subsequent batches.
- Read-only instrumentation delegates to the existing evidence resolver/calculator without changing returned data or calculating prices twice. It records every lost stored identity with typed source status, issue codes, source IDs, and bounded missing-fact excerpts (`REVIEW_LOST`), plus every old/new aggregate median and contributor count (`REVIEW_AGGREGATE`). The existing CLI still produces its normal preview. A local query-only test matched the independently checked June 1 quarterly/5,000-kWh median exactly; its log is `/tmp/annual-v2-reviewed-preview-local.log`.
- CLI session for this release and preview: `voltikka-quarter-reference-release-20260912-1`, caller `skill:use-railway@1.2.2`.
- No historical apply or public activation is authorized or performed. Scheduled current collection can independently start writing corrected v2 rows after deployment; recheck current coverage after the preview instead of assuming v2 remains empty.

## Corrected preview reviewed; current-date refresh still required
- See `preview-f651849.md` for the complete 233-date review, all 1,653 lost identities, aggregate changes, and current-date checks. Candidate history has 213,755 annual rows and 7,618 aggregates; no aggregate disappears or crosses below the 10-member sample floor.
- Current v2 was independently collected by the scheduler for September 12. Its 705 annual values and 30 aggregate medians/counts match current snapshots, but a strict read-only provenance check failed on 63 missing interpretation references across 21 contracts. All 21 matching interpretations are now published, after the stored statistics were calculated. Publication alone does not update those stored rows.
- The validation exit 255 was the diagnostic script's explicit failed assertion, not an application-service crash. Details are in `/tmp/annual-v2-current-validation.log` and `/tmp/annual-v2-current-provenance-diagnostic.log`.
- Read-only code agent `3516c173-c512-4e8` confirmed that import retains old published canonical JSON, dispatches analysis, and calculates statistics without waiting. The current calculator can use that retained JSON while the provenance join rejects the old interpretation against the new pointed snapshot. Numeric agreement with snapshots alone does not prove current source publication.
- Scheduled 07:30 forecast recovery is conditional and fixed-term scoped: the sole failure must be `statistics_publication_order`, the run must be non-dry, and active contracts must exist. It then recalculates statistics for all current active IDs and checks freshness again. Other failures defer the run without recovery. Do not describe this as an unconditional full-market repair loop.
- Production at 07:26 had 384 active contracts, 278 public-target active contracts, and no unmatched publication among those 278. The stored current rows still had 63 missing interpretation references. At 07:29:34 the read-only forecast gate reported only `statistics_publication_order`, so the recovery condition was met then. Completion still requires checking stored data after the scheduled run. Logs: `/tmp/annual-v2-current-followup.log` and `/tmp/annual-v2-current-forecast-gate.log`.
- No manual current refresh, historical apply, or activation is approved.

## Current refresh passed and historical apply approved
- At 07:32:45 Helsinki, after the scheduled refresh time, the current v2 check passed with 789 rows, 30 matching aggregates, complete matching source provenance, and public v1 retained. No manual refresh was run. See `/tmp/annual-v2-current-after-schedule.log`.
- After the Voltikka status update, the user explicitly approved application of the reviewed historical v2 data. Scope is the nine monthly ranges in `apply-plan.md`, January 21 through September 11, on Railway Voltikka / production / voltikka. Public activation remains a separate approval.
- Exact deployed SHA f651849 and successful deployment 67088a70 were rechecked. CLI authentication and the Voltikka checkout were rechecked; the unrelated cb virtual-environment command did not persist in these shell commands.
- The operator's prior full-backup verification remains the backup approval evidence. A new read-only object-metadata check found the latest archive `Voltikka/voltikka-2026-09-12-00-01-33.zip`, 37,683,935 bytes, with an archive password configured. Eight latest-named ZIP files were inspected among 35. This is not an independent restore or content verification. Log: `/tmp/annual-v2-apply-backup-metadata.log`.
- Read-only baseline fingerprints are in `/tmp/annual-v2-apply-baseline.json`. They cover all 196,256 v1 annual rows, 6,639 v1 annual aggregates, 21,114 non-v2 historical statistic rows, 75,413 historical snapshots, and 239,811 historical price components. No historical v2 rows/aggregates existed in the target range, and neither target write table has a trigger. Helper: `/tmp/annual-v2-protected-fingerprints.php`.
- Temporary guarded apply tooling is being prepared by executor `7d973710-b7be-459`; it must not run production commands. Parent reviews and executes. Each date must match its reviewed comparisons before writes; each completed batch must preserve the protected baseline hashes. Session: `voltikka-annual-v2-apply-20260912-1`.
- Guard tooling was reviewed before execution. Parent independently matched all 233 embedded manifest dates to the original preview records. Final local checks: 270 SQL/writer checks, eight runner stub cases, and actual query-only January 21 / June 1 calculations under the SELECT guard. A parent-found SSH-stderr/JSON parsing defect was fixed before any production apply; helper stdout and stderr now have separate bounded files.
- Approved apply started in background job `job-55742-144` (`annual-v2-approved-historical-apply`). Command: `python3 /tmp/annual-v2-guarded-apply-batches.py --execute-approved`. Job output: `/tmp/pi-bg/job-55742-144.log`. Per-batch logs: `/tmp/annual-v2-guarded-apply-<FROM>-<TO>.log`; post-batch fingerprints use the `.protected.json` suffix, with separate `.protected.stderr.log` files. Wait for completion notification; do not start another apply or retry blindly.
- PHP guard SHA256 at launch: `5100a7789db6c644d57efc7d1a2a36bef392d28bb56f40a80b528071f04caebf`. Details: `/tmp/annual-v2-apply-tooling-notes.md`. Every date must match the reviewed medians/counts/methods/loss set before the original writer can run; all stored non-timestamp fields are checked after each date. This is not a claim that the preview hashed every available row or every input. SQL guard permits only exact verified v2 writer statements. The original writer owns per-date commits.
- At this entry, the apply job is running; completed dates/results are not yet known. Approval does not include public activation or any current-date overwrite.

## Transport interruption and verified May completion
- Local job `job-55742-144` exited 1 after 30m12s. January through April had complete per-date/batch markers and matching protected fingerprints. The local May log stopped after May 1, but this did NOT mean the remote PHP process stopped.
- Read-only database inspection found subsequent May dates committed. `/proc` command classification identified the existing guarded May 1–31 process as PID 1034 under timeout PID 1033. No retry or overlapping apply was started. A bounded `tail --pid=1034` wait confirmed that process exited.
- Final post-May fingerprints match the original protected baseline exactly. History now contains 125,556 v2 annual rows and 4,444 aggregates through May 31; June onward is not applied. See `/tmp/annual-v2-after-may-fingerprints.json`.
- Independent read-only May verification passed at 08:43:32 Helsinki: all 31 dates, 28,538 annual rows, 1,015 aggregates, reviewed contributor counts/medians/methods/lost identity sets. The final May 31 date was recalculated and every stored non-timestamp field compared with the original writer preparation. Public v1 remained active. Proof: `/tmp/annual-v2-after-may-verification.json`; implementation `/tmp/annual-v2-verify-may-body.php`.
- The original script could discard error output if calculation threw while its diagnostic output buffer was open. This diagnostic defect was repaired with try/finally cleanup. SSH return codes are now recorded distinctly. Neither defect proves the cause of this connection interruption: local May 2 passed, and production completed all May dates. Pricing code, SQL guard, and reviewed manifest were not weakened.
- New scratch executor `20097ef8-8846-4c3` prepared remaining June, July, August, and September 1–11 batches only. Parent reviewed the runner and reran 25 stub scenarios, shell output/exit/no-overwrite tests, and all four decoded date-manifest checks. Existing approval covers these exact remaining ranges; no earlier date is resubmitted.
- Remaining apply started as job `job-55742-156` (`annual-v2-remaining-approved-apply`), command `python3 /tmp/annual-v2-resume-batches.py --execute-approved`. Job output: `/tmp/pi-bg/job-55742-156.log`. Local logs: `/tmp/annual-v2-resume-<FROM>-<TO>.log`. Remote durable logs: `/tmp/voltikka-annual-v2-f651849-<FROM>-<TO>.log`.
- Runner SHA256: `f2dbc7065ad8ba24a6fcc2f482d7353a7362b2e8df658d76e89f567b22267d6e`; repaired PHP guard SHA256: `67e017f1233c4ee9b4bf40256d6ba655eabb1b59e7b7d0f50a899b6bbe0d07ab`. Notes: `/tmp/annual-v2-resume-notes.md`.
- The foreground remote shell reserves its log without overwrite, redirects PHP output there, and appends `REMOTE_APPLY_EXIT_CODE`. The local runner records a distinct `SSH_TRANSPORT_EXIT_CODE`. A connection failure still requires checking remote process state and reading that server log before any retry. No automatic retry or overlapping apply is allowed. Each successful remaining batch must still match all protected baseline hashes. Public activation remains unapproved.

## July transport interruption; completed database batch
- Remaining job `job-55742-156` stopped after 14m20s. June completed with all markers, remote exit 0, and matching protected fingerprints. July returned SSH exit 255 with no application output in the local file.
- Server log `/tmp/voltikka-annual-v2-f651849-2026-07-01-2026-07-31.log` continued advancing. Existing PHP PID 1215 remained active under timeout PID 1214; the timeout had been reparented to PID 1. No duplicate apply was issued.
- Parent verified PID 1215's stdout belonged to that July log, waited for its exit, and copied the bounded server log to `/tmp/annual-v2-recovered-july-server.log`. It contains all 31 review/apply markers and the final PHP `BATCH_APPLY_VERIFIED` marker, with 27,188 annual rows and 951 aggregates. The shell's remote-exit marker is absent: that marker cannot be relied on after a lost parent shell, even when PHP completes.
- Post-July protected fingerprints match the initial baseline. Historical totals are 179,837 annual rows and 6,385 aggregates through July 31 (191 dates). Proof: `/tmp/annual-v2-after-july-fingerprints.json`, successful job `job-55742-160`.
- Scratch executor `34642843-2982-41f` prepared August 1–31 and September 1–11 only. Parent reviewed `/tmp/annual-v2-final-review.diff` and reran all 37 stub scenarios, five scope refusals, decoded manifests, and isolated GNU tee shell tests (live/file output, nonzero PHP, tee failure, existing log, closed stdout pipe). The unchanged PHP guard SHA is pinned. The runner requires recovered July completion and post-July fingerprints and hashes those artifacts between batches.
- Final job `job-55742-162` (`annual-v2-final-approved-batches`) started with `python3 /tmp/annual-v2-final-batches.py --execute-approved`, under the original approval. Output: `/tmp/pi-bg/job-55742-162.log`; local per-batch files: `/tmp/annual-v2-final-<FROM>-<TO>.log`; server files retain `/tmp/voltikka-annual-v2-f651849-<FROM>-<TO>.log`.
- An explicit bash foreground pipeline uses GNU `tee --output-error=warn-nopipe` to stream and save PHP output, with immediate PIPESTATUS capture and distinct PHP/tee exit markers. Live output may prevent an idle connection failure, but the cause is unproven. A disconnect still stops the runner and requires remote process/log inspection. No automatic retry is permitted. Public v1 remains active.

## Historical application complete; activation awaits approval
- Final job `job-55742-162` passed August and September with zero PHP/tee exits and matching protected fingerprints.
- Independent whole-history read-only verification `job-55742-167` passed all 233 dates, 213,755 annual rows, and 7,618 aggregates at 09:37:57 Helsinki. The full-date row counts and reviewed aggregate/count/median/method/lost sets match. The final September 11 date's complete non-timestamp stored fields also match a fresh calculation.
- Current-data check `job-55742-164` passed 789 rows / 30 aggregates, including current observation/publication pointers and complete provenance. Candidate reader check `job-55742-166` used process-local v2 plus array cache only: v2-only annual rows, September 12 annual/unit endpoints, seven consumption rows, 68.5 MiB peak memory. Public v1 is unchanged and the statistics page returns HTTP 200.
- See `apply-results.md` for proof paths, limits, and the exact proposed Railway variable operation. Historical application is complete; public activation has NOT been approved or executed.

## Public v2 approved and active; CSV follow-up open
- User approved activation. Fresh current verification passed, then the one approved MCP variable write triggered deployment `bcb5a9e7-2b88-4841-a97c-08708d990eb9` at the existing f651849 SHA. Exact poll job `job-55742-174` reported SUCCESS.
- Effective cached public v2, 213,755 historical rows / 7,618 aggregates, and all protected baseline hashes passed. Current 789 rows / 30 aggregates and observation/publication pointers passed. Statistics, consumption, and Helen Oy readers select September 12; process-local v1 switchback selects retained September 11. Unit/index payloads are unchanged. Real public pages return 200.
- CSV verification found a real 30-second execution timeout: HTTP 200 followed by partial CSV and a 500 HTML footer. Application log points to `Connection.php:412` during repeated sorted chunk/OFFSET queries. This is not an annual-calculation mismatch. A local streaming fix is assigned to `9e03d71b-2276-416`; a separate code release must be approved before deployment. No rollback or timeout mutation is authorized.
- See `activation-results.md` for full results, diagnostic-only company lookup mistakes, and proof paths. Keep the final verification task open until the complete public CSV passes.

## CSV fix ready for separate release approval
- Parent reviewed executor `9e03d71b-2276-416`'s actual sorted, unbuffered cursor implementation and restoration tests. Public CSV semantics and all-version rows remain unchanged.
- Guarded production read-only candidate benchmark (not an installed route): all 28,819 rows / 7,648 active v2 aggregates in 14.424 seconds at 36.5 MiB under the existing 30-second / 128 MiB limits. Exact row count/order/18-column shape/latest date passed; all 23,500 complete rows from the old partial public CSV match every field. PDO buffering was restored and a later query worked.
- Full suite: 2,285 tests / 10,862 assertions; asset build and diff checks passed. Evidence and release scope are in `tasks/contract-statistics-csv-streaming-fix/decisions.md`.
- No code push or production installation occurred. Request separate approval for `git push origin main` to auto-deploy Voltikka / production / voltikka, then verify the exact deployment and complete public HTTP CSV. V2 remains active; no historical overwrite or timeout change is part of this release.

## Rollout complete — CSV release and final verification passed
- User explicitly approved push/deploy. Commit `722582c71a32fbaa4eb329b98b69d08539c9a74d` was pushed from main; exact deployment `6ce82497-7275-4402-babd-dcbad23127e1` reached SUCCESS (poll job `job-55742-194`).
- Full public CSV: HTTP 200, 28,819 complete rows, 7,648 active v2 aggregates, 6.811 seconds. Every field matches the read-only candidate. Core public pages and all four CSS/JS assets pass; assets match the production manifest and its CSS dependency lists.
- Exact-deployment cached public v2, current provenance, statistics/consumption/Helen Oy readers, retained September 11 v1, and unchanged unit/index payloads pass. Final protected fingerprints at 20:04:53 Helsinki match the original baseline; historical v2 remains 213,755 rows / 7,618 aggregates.
- The final rollout task is complete. See `activation-results.md` and `tasks/contract-statistics-csv-streaming-fix/decisions.md` for artifacts. Result notes remain local; no extra documentation-only push, timeout change, historical rebuild, or rollback occurred.

## General review requirements
- Merge only completed date summaries, ensure no duplicate or omitted available evidence dates, and report partial coverage if any batch fails.
- Check new/lost contract-consumption identities, aggregate coverage, unavailable reasons, and significant deltas. The first portion includes a maximum matched aggregate median increase of about €1,799 on 2026-04-08; this is across multiple consumption levels, not a 5,000-kWh claim. Inspect the segment and estimation basis before accepting it.
- The old local sample is not a substitute for the production preview. Its zero-placeholder Hybrid regressions are already fixed in the release.
- Resolve today's missing v2 coverage before activation. V1 currently includes September 11, while historical command execution on September 11 stops at September 10. Do not call a dated v2 endpoint today's comparison or overwrite v1 snapshot provenance merely to advance it.
- State exact project/environment/service, apply command and date range, expected changes, and backup/activation boundaries; wait for affirmative approval before production writes.
