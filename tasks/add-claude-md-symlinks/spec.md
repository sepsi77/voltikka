# Add CLAUDE.md context-file symlinks

Ensure each repository directory that has an `AGENTS.md` context file also has a corresponding `CLAUDE.md` file with identical content. Prefer symlinks to avoid duplicate documentation drift.
