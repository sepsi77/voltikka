# Spot 12m statistics display

Update `/sahkosopimus/tilastot` contract-type energy price table so spot contracts use a more meaningful 12-month realized spot comparison instead of the latest daily spot price. Show p20–p80 daily-price range under the spot energy price without adding a new table column. Update explanation copy accordingly.
