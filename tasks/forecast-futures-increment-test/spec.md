# Frozen futures-only increment protocol

Freeze before calculation. Offline standard-library Python only; write only this new folder. Preserve all prior artifacts. Use the complete export export-20260913T094138Z under supplier-premium-coverage. Pin all export files, reused helper sources, prior specs, and forecasting rule sources before results. No network, database, dependency, application change or release.

Primary median, exact 30 calendar days; secondary median 14 days for additional falling evidence, never a replacement. Independently fit 6/12/24 months. Expanding training labels strictly before each issue; minimum 20 unique starts for unrestricted mean and feature fit. Observed prefix ends at first canonical statistic presence per term (including invalid presence), then canonical continuation. Latest ID owns each basis/date before finite median validation. Both endpoints must be on the same owned basis; no cross-seam pairs, observed fallback, date fill, p20/vector gate or retail-lag requirement.

Primary x is seven-calendar-day FI futures change. Both valuations use the identical next-full-month(issue) duration basket. Latest trade strictly before issue and issue-7 respectively; reuse basket(), VAT 1.255, actual month days, month/quarter/year fallback, complete strips. Secondary rolling-delivery x uses each endpoint's next-month basket; label separately. Each cohort has its own feature-eligible training and tests; all five models share identical evaluable test targets within cohort. Do not require all terms on a date.

Models: unchanged delta 0; production mean from ALL eligible completed pairs; feature-cohort mean; feature-cohort intercept plus futures; production mean plus futures. Ridge penalty fixed 1 on training-population-standardized x, unpenalized intercept, centered feature-cohort outcomes, reuse fit/predict. Hybrid delta = unrestricted mean + slope*(x-train_mean)/train_std. Constant feature contributes zero. No tuning or additional model/window/threshold search.

Apply production-style half-up four-decimal delta, then four-decimal current+delta price to every model. Numerical error compares saved forecast with exact target. Actual direction uses four-decimal target minus saved current. Both directions use inclusive +/-0.15 c/kWh, otherwise flat.

Save compact nonduplicated pair, basket, fit, prediction and availability evidence: raw statistic IDs, basis, dates, all training IDs, maximum label date, feature means/scales/slopes, selected trade dates/instruments/weights/settlements. Split MAE, bias, correct/missed/wrong/false by horizon/cohort, term/all, basis/all and actual up/flat/down/all. Include N, unique issues and period bounds. Paired absolute-error improvement is control error minus model error vs unchanged, production mean and feature mean, also grouped by issue.

Verify chronology, exact dates, median-only eligibility, invalid latest ownership, seam, every term, matched cohorts, signed threshold, missing lag, month roll, independent mean/slope and basket arithmetic. Use first and last evaluable primary fixed fits per term for independent checks; fixed first fit per term synthetic x=-1,0,+1 sensitivity is behavior NOT accuracy. Run twice, compare byte hashes and original input hashes before/after.

Limits: overlapping windows; revised export vintages; previously inspected period, no untouched holdout; observed/canonical continuity assumption; report falling counts even if zero and do not infer falling accuracy from sensitivity. April 8 export cutoff excludes January–April production history: compare the production RULE, not exact production 174-pair vintages. Do not claim broad accuracy or credibility. Recommend no adoption unless evidence supports it across regimes; even favorable results need independent future evidence.
