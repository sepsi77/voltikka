# Decisions

- Freeze `spec.md` before computing new metrics. Primary input is seven days and primary target is 30 days. Fourteen-day input and target are separate predeclared secondary tests.
- Reuse the prior verified export loader, owned public-median selection and hedge mathematics by read-only Python import. Reuse verified exact fixed-gap forecast rows; do not run a writer in the previous task.
- All new outputs stay in this folder. Existing application changes and previous task folders were already in the working tree and are not ours.
- No controlled-index construction, production access, database access, new dependency or release action.
- Paired training also requires the verified fixed-gap reference. This starts seven-day-input training on April 19 (10 prior reference history days); fourteen-day input starts April 23 (strict prior trade at the old endpoint). All learned models have identical training and test rows.
- Frozen training has 69/85 unique observed issue days for seven-day inputs at 30/14-day targets, and 65/81 for fourteen-day inputs. All latest labels are July 26, strictly before July 27. Exact IDs, training means/scales and standardized/raw coefficients remain in artifacts.
- Primary canonical test has 36 rows on 12 days, August 3–14. MAE: unchanged .608625; gap .656925; retail .479421289; rolling .436333313; fixed basket .485760964. Fixed basket loses to both learned controls. Abstention changes zero primary rows. All 66 pairs of issue windows overlap.
- Secondary seven-day-input / 14-day target has 84 rows on 28 days. MAE: unchanged .289119048; gap .318807143; retail .318988408; rolling .306563816; basket .328628938; abstention .335657839. Abstention changes six rows on six days and makes MAE worse. All alternatives lose to unchanged on MAE, but learned RMSE is lower.
- Fourteen-day-input / 30-day sensitivity retains only five issue days, as predeclared. MAE: unchanged .69904; gap .739333333; retail .317331965; rolling .248909870; basket/abstention .325230398. This selected result cannot replace the primary cohort.
- Strictly canonical rolling origin is unavailable: maximum 13 matured prior issue days, below the fixed 20-day gate. Observed seven-day-input / 30-day rolling origin has 19 test days: unchanged MAE .471821053 versus basket .572525713, rolling .576215442 and retail .640128311. This older regime does not confirm the frozen transfer gain.
- Primary gains come from the six-month term; all learned models lose to unchanged for 12 and 24 months. Large positive training intercepts carry much of the predicted change. An intercept-only control was not predeclared and was not added after results. The report proposes freezing it before any new prospective labels if desired.
- Independent verification passed: 729 basket vintages, 474 same-vintage checks against prior actual-PHP-verified H, 954 features, 1,098 pairs, 136 three-model fit sets, 3,564 predictions, full identity/cutoff/metric checks and nine synthetic edge-case checks. No test failed during this task's verification runs.
- Final `scripts/reproduce.py` passed two runs: 13 byte-identical report/artifact outputs and all nine unchanged export files. `git diff --check` passed. No Laravel tests were needed or run because no application code changed. Reused prior PHP results were checked by their recorded hashes, not rerun.
- No production archived-vintage collector is deployed for this experiment. The report gives a proposed canonical-only prospective contract with immutable input/target capture, frozen gates and one predeclared evidence review, but adds no dated follow-up or schedule.
- The holdout was already inspected before this task. All findings remain retrospective and contaminated by prior analysis; revised export-time records do not prove original-vintage availability. No coefficient, model, horizon or application default was selected for production.

## User-approved post-hoc intercept continuation

- The user then approved an intercept-only paired control. The earlier decision not to insert it into the original predeclared model set remains valid: this continuation uses new spec/checksum, scripts, report and artifact paths. The original frozen specification, scripts and every existing report/artifact file remain byte-identical, pinned in `intercept-inputs.json` before calculation.
- Freeze `intercept-spec.md` before new metrics. Use exactly the original three learned models' training IDs, with equal row weights across 6/12/24 months. Predict current plus historical mean target change. No term-specific means or feature-free cohort expansion. Recomputed means equal all stored standardized intercepts within 1e-10; raw-feature b0 is not the baseline. Original learned predictions are not refit.
- Primary mean change is 0.604894 c/kWh. Intercept MAE is 0.484324 versus unchanged 0.608625. Retail improves on intercept by 0.004902 (1.01% relative MAE); rolling by 0.047990 (9.91%); basket loses by 0.001437 (0.30%). Thus most of retail's error reduction versus unchanged is reproduced by the mean. These are error comparisons, not causal fractions or variance explained.
- Primary intercept MAEs for 6/12/24 months are 0.729147 / 0.319154 / 0.404669. Rolling MAEs are 0.614494 / 0.297359 / 0.397147. Rolling beats intercept in each term, but intercept and every learned model beat unchanged only for six months. No term-specific model is selected.
- Seven-day inputs, frozen 14-day target: intercept MAE 0.315398, rolling 0.306564; both lose to unchanged 0.289119. Older observed rolling origin: 30-day intercept 0.438400 versus rolling 0.576215; 14-day intercept 0.439346 versus rolling 0.391073. Fourteen-day inputs keep the same signs versus intercept in both regimes/targets. Feature benefit does not transfer consistently across targets and regimes.
- Independent verification passed for 1,098 raw pairs, 136 fit means, all 3,564 unchanged original prediction rows, 594 added intercept rows, 224 full-cohort metric groups and 112 common-cohort groups. It checks exact statistic/training/test IDs, balanced row/issue weights, strict cutoffs, 20-day gates, source hashes, all errors/adjustments/metrics, and constant-delta/changing-cohort synthetic data. Original verification also passed with writes disabled; pinned PHP/H evidence is reused without database access.
- `python3 tasks/forecast-accuracy-experiments/scripts/reproduce_intercept.py` passed two complete additive runs with eight byte-identical outputs and unchanged pinned original/export/source files. `git diff --check` passed. No Laravel test or build applies because application/CSS/JS files were not changed by this continuation.
- Keep the same limitations: inspected holdout, 12 primary issue days with all 66 forecast-window pairs overlapping, dependent terms, observed-to-canonical continuity assumption and possible export revisions. Strict canonical rolling remains unavailable. No production recommendation, collector, prospective experiment, schedule, dependency, commit or push follows.
