# Sentry N+1 price components on SEO location pages

Investigate Sentry N+1 query report for redundant `price_components` queries in `seo-contracts-list` on `/sahkosopimus/paikkakunnat/{location}`, determine whether this is avoidable without reintroducing full price-history memory issues, and implement a safe optimization.
