# Frozen direction sensitivity specification

Research only. All new files stay in this folder. No application/configuration/database/production/dependency/commit changes. Preserve all previous work. Use the verified export `tasks/supplier-premium-coverage/export-20260913T094138Z`, saved repaired replay, and existing learned/intercept predictions and fits. No new fit, lambda change (0.30), term selection, or wider export.

Freeze this file and SHA-256 before new metrics. Prediction thresholds are exactly 0.15, 0.10 and 0.05 c/kWh, inclusive at both signed boundaries. Actual outcome threshold stays 0.15. Use finite Decimal arithmetic. Actual target minus current is rounded to four decimals, half away from zero, before classification. Use model-published expected change where available: continuity replay JSON `expected_change_cents_per_kwh` for v3, exported stored expected change for v2. Cross-check rounded forecast minus current within 0.0001. For learned and intercept predictions subtract the original unrounded prediction price and current as Decimal, without additional rounding; retain the original prediction_delta for a precision cross-check. The gap reference uses published replay expected change when the exact 30-day canonical replay identity exists; otherwise retain its published four-decimal forecast minus current (the earlier horizon reference has no exported expected-change field). Disclose that fallback. Preserve old saved-label baseline separately, with unrounded Decimal actual difference, and list any saved/numeric category disagreements.

Audit all 33 saved-v3 missed moves in the matched median 48 rows: 16 issue days July 27–August 14, excluding August 2–4 without v2. List IDs, dates, terms, current, target, predicted prices and deltas, saved and numeric directions. Split positive, zero and negative deltas. Count recoverable moves and wrong-sign amplification for each threshold. Separately evaluate full v3 mature median 57 rows (19 days), never compare aggregate scores across cohorts.

Learned comparisons use the exact original seven-day-input cohorts: primary frozen canonical 30-day target, 36 rows/12 days; older observed rolling-origin 30-day, 57 rows/19 days; secondary frozen canonical 14-day, 84 rows/28 days. Models: unchanged, intercept, repaired gap, retail, rolling futures, basket. Add fixed always-UP and always-DOWN direction-only controls: hindsight-free rules, not numeric price forecasts and not production candidates. No abstention or 14-day input variants. Pin exact prediction and fit IDs; verify training targets strictly precede cutoffs without refitting.

For each model, cohort, threshold and term/all: exhaustive correct/missed_move/false_move/wrong_way counts and rates, all nine actual-by-predicted confusion cells, true class balance, issue-day counts, up/down precision and recall with explicit numerators and denominators (null if denominator zero). Balanced accuracy across all three classes is unavailable unless all classes occur. Full 4x4 outcome transitions and 3x3 prediction transitions from 0.15 to each lower threshold must conserve every row. Include rescued misses, new false moves, new wrong ways and correct rows lost.

Deliver standard-library analysis and independent verification, row-level CSV, metrics/transition CSV and JSON, markdown report, pinned input hashes, preserved-source checks, failed/missing/skipped counts and two byte-identical runs. Verify raw statistic IDs/prices and snapshot ownership where available, original v3 fields, exact model maps, Decimal boundaries including zero, and metric/transition conservation independently. Do not run previous writers.

Interpret this as post-hoc sensitivity, not fresh validation. All 66 primary window pairs overlap; terms share shocks; the holdout was inspected; export revisions and observed-to-canonical transfer remain limits. Canonical rolling training is unavailable under the existing 20-day gate. Report practical low-risk trial candidates without claiming proof or starting a trial. Any later signal-threshold release needs explicit approval, separate signal/outcome metadata and model identity; the current application reuses one saved threshold for both, so do not change historical directions or redefine accuracy.
