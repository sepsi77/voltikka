# Decisions

- User approved an offline post-hoc threshold diagnostic, not a release or a model change.
- Freeze exact Decimal field rules and cohorts in spec.md before metrics. Use original v3 expected change rather than reconstructing it from separately rounded prices. Learned prices retain their saved arithmetic precision.
- Existing application and research changes are not ours. Pin all existing task files and tracked working-tree files before calculations; verify them again after both runs.
- Complete: all new files remain in this folder. The unchanged frozen spec and 1,886 pinned previous files pass all checks.
- The 33 matched saved-v3 misses split into six positive, zero zero and 27 negative predictions. Neither 0.10 nor 0.05 rescues a miss. V3 matched correct/missed/false/wrong counts are 13/33/0/2 at 0.15, 10/29/3/6 at 0.10 and 8/18/5/17 at 0.05. Keep the existing v3 signal threshold; the main cause is wrong sign, not small positive signals.
- Saved-label baselines reproduce exactly. No saved/numeric category disagreement occurs. Separately rounded prices can differ from published expected change by 0.0001; the original field owns v3 classification.
- Actual code inspection corrects the initial manager description: evaluation rounds actual change to four decimals. Generation still passes unrounded lambda × gap to directionLabel before storing four-decimal expected change. No application repair is in scope. This distinction does not change selected-row results.
- Primary 36-row learned/intercept 75% is exactly the fixed always-UP score: 27 up and nine flat actuals, no down. Older 57-row intercept/retail equal always-UP at 39/57; secondary 84-row intercept/retail equal always-UP at 38/84 and lose to unchanged 45/84. Fixed-direction controls do not supply numeric forecasts or establish fall performance.
- All selected fits and predictions remain unchanged. Twenty-one exact fit sets have raw training means, strict cutoff chronology and the 20-day gate checked again. No canonical rolling fit is invented. Prior inspected/overlapping windows and export-vintage limitations remain explicit.
- Verification: `python3 tasks/forecast-direction-sensitivity/analyze.py` passed. `python3 tasks/forecast-direction-sensitivity/verify.py` passed. Final `python3 tasks/forecast-direction-sensitivity/reproduce.py` passed two runs with 15 byte-identical outputs and 1,886 preserved files. Evidence: results/verification.json, results/reproducibility.json and reproduction.log. Checks cover 3,348 raw statistic price uses, all 477 snapshot median/count groups (zero differences), 348 metric groups, 232 transition groups and 22 synthetic edge cases. No verification failure occurred.
- Missing selected rows: zero. Nine mature median v2 dates/terms are absent; 90 immature v3 median rows, 294 other-quantile generated rows and 3,096 out-of-scope learned source rows are explicitly excluded. No wider export is needed.
- `git diff --check` passed. Final status review shows only this new task folder in addition to all preserved previous changes. No Laravel test or asset build applies to this research-only change. No app/config/DB/production/dependency/commit/push action occurred.
- A separately approved small shadow trial of mean/retail/rolling indicators is possible, but this test does not choose a production model. Do not start a trial or lower the v3 threshold automatically. Any later release needs separate signal/outcome metadata and model identity, while preserving the 0.15 outcome target and historical labels.
