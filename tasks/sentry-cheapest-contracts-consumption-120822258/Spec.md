# Fix Livewire consumption property error on CheapestContracts

Sentry Issue 120822258: Livewire PropertyNotFoundException for `$consumption` on `cheapest-contracts` when `CheapestContracts` calls parent `SeoContractsList::getContractsProperty()`.
