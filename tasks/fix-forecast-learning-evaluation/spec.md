# Local forecast learning and evaluation fix

Supersede the initial canonical-only learning plan. Retain valid older unit history as an explicit continuity assumption: observed observations before the same term's first canonical unit date can initialize EWMA; subsequent canonical observations update it. Current exact-date inputs remain strict to PricingMode. All reads use unit_statistics_v1, energy_price, the same term, and null consumption. Derive the boundary from canonical presence no later than asOf, including invalid values. Current and future values never enter history. Latest-ID ownership precedes validation; no observed fallback after the boundary, including absent or invalid canonical dates. Feature-off remains observed-only.

Keep original basis counts and accepted date bounds. Store the versioned continuity policy and transition date. The minimum 10 uses combined accepted days. Store confidence_history_observations and use accepted current-basis days only for unchanged 120/365 confidence thresholds. Keep alpha 0.25 and lambda 0.30 unchanged. Ordinary General prices are comparable unit quantities, not annual energy averages, but discounts, corrected rates, eligibility, and classification can differ. Do not claim perfect equivalence. The September 12 annual_cost_as_of_v2 change is unrelated. Canonical-only learning remains a separate offline validation alternative; no runtime flag or schema change.

Retain model v3 identity/defaults and reserved v1/v2 guards without rewriting old rows. Keep all evaluation safeguards: saved basis, unit method, threshold, finite exact-date latest-ID actuals, unchanged-price baseline, stable ID iteration, completed-row preservation, and read-only dry run. V1 cross-basis targets remain missing; valid canonical targets can evaluate v2 canonical-current rows.

Production remains v2. No production access, deployment, commit, or push. Do not change tasks/forecast-stability-review or tasks/supplier-premium-coverage. The separate authorized replay unit executes the actual revised PHP services against the complete verified export-20260913T094138Z in isolated in-memory SQLite fixtures. It writes only task replay artifacts and task state, never app code or the original export. Compare continuity, a separate canonical-only fixture, exact-date stored-v2 forecasts and the unchanged-price baseline. Prove original dry-run/completed-row preservation and independent H/EWMA/error results. Report retrospective export-time limits and avoid superiority claims. The initial nine-matured-date claim applies only to the canonical-only counterfactual; the measured continuity result is 19 dates per term/quantile. The manager also verified the finalized continuity code with the full suite.
