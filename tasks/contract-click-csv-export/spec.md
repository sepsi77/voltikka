# Contract click CSV export

Add a CSV download to the private Filament contract-order-click list. The export must contain every database column and every stored click row. Only an authenticated administrator can download it.
