# Final verification

## Current release gate — default-V4 technically ready for user deployment approval, 2026-09-16

See [child release readiness](../source-validated-energy-rules/release-readiness.md) for current
verified facts, exact hashes, comparison and UI logs, and explicit video limits. Independent review
verified 81 totals, 37 anchors and 39 rates/fees. The manager accepts the 27 numeric changes as
policy-conformant, not empirically better forecasts. Fresh strict transport passes 375/375 in
all four phases; candidate lists 366 with 29 changed total/states, including Kerava's new exclusion.
Runtime and fixture repairs are complete; final UI job 228 and full synthetic video/template review
pass. Final network-denied PHP job 235 passes: **2812 tests / 21214 assertions / 106.56 seconds**,
log `/tmp/voltikka-release-final-php.log`; preflight/export smoke also pass. The manager accepts
default-V4 technical readiness for user deployment approval with no technical conditions remaining.
Job 237 passed Pint for 140 changed/new Laravel PHP files and all five preflight PHP files, plus
`git diff --check`. Subsequent manager checks passed: 22 context pairs, both task JSONs, whitespace,
all 84 loaded-source hashes and both unchanged sealed SQLite hashes. V4 assets match HEAD; current
producer config is unchanged. See the child `release-readiness.md` for complete results. No runtime blockers remain. No full HTTP,
live-cache or ranking replay is claimed. Production has not received the local fixes.
No V5 activation, deployment, historical rebuild or posting is authorized; V5 producer accuracy
remains separate and unmeasured.

The records below are superseded dated evidence. Their earlier pending amount/video statements
are not the current status, and their suite counts do not verify the final tree.

## Historical release gate — BLOCKED after earlier local repairs, 2026-09-16

The child NULL-reader repair, public-copy and estimate-disclosure fixes, sealed replay and SQLite
query diagnosis are complete. See [audit fixes](../source-validated-energy-rules/audit-fixes.md).
Final manager verification: **2773 tests / 18253 assertions / 138.45 seconds**; Laravel build and
latest Remotion lint/TypeScript/build passed. Logs: `/tmp/voltikka-audit-final-{full,build,remotion-lint,remotion-build}.log`.
Local calculated-cost schema is 19; deployed baseline is 17. All application units, export cleanup
and bounded video repair/12-PNG QA are complete. The final same-sealed-database replay resolves
legacy no-discount and overlap-end anchor defects: 375 rows per consumption, identical cold/warm
business rows, 367 listed, 28 changed displayed states. Remaining premium/unknown-restart amounts
need economic review; remaining policy and Supplier/Reset disclosure/guard decisions stay open.
The report-only benefit cent-comparison fix passes 14 smoke cases and preserves raw fields.
The corrected `audit-fixes-reviewed-comparison.json` has two benefit transport changes at each
consumption. Manager Pint passed all 131 changed/untracked Laravel PHP files.
Bounded video QA is not real-feed/full-video/phone/compression approval. V5 activation remains separate.
The original failed production-data gate and all dated test results below remain factual history.

## Historical manager acceptance — completed local implementation

Full-policy implementation and LOCAL PHP/public-output acceptance are complete after schema 19.
See [the current child acceptance](../source-validated-energy-rules/final-verification.md) for review
scope, exact logs and limits: **2754 tests / 17909 assertions / 135.13 seconds**, Laravel build
**836 ms**, and Pint on **all 122 changed/untracked PHP files** passed. The manager's whitespace,
19 context pairs, three task JSONs and unchanged V4/V19 assets/canonical config checks also passed.

Remotion ESLint, TypeScript and bundle checks subsequently passed after the approved dependency
install. Visual verification remains pending; no render was done. The earlier offline failure
remains historical evidence in the child `video-output-check.md`.

This is not production acceptance. Defaults remain V4; deploying this tree does not activate V5.
The fresh read-only production preflight is complete but FAILED its release gate. The coordinated
five-key profile switch, V5 pilot, fresh activation tests and reinterpretation require separate
approval. No production mutation or commit/push occurred. See the child `release-plan.md`. All earlier records below are historical, including their
then-pending scope; they do not override this current status.

## Historical stage — bounded source-backed energy acceptance

The user approved local source-backed guarantees and adjustable-discount semantics, not inferred
guarantees. The manager has now accepted bounded source hardening, paired kernel corrections and
inactive normal-episode evidence. See [the new verification record](../source-validated-energy-rules/final-verification.md)
for exact scope, logs and limits. The fresh combined gate passed: **2707 tests, 17217 assertions,
128.90 seconds**; build passed in **844 ms**; Pint passed on **104 changed/untracked PHP files**.
These are manager-supplied results, not new runs by the documentation executor.

Real-language coverage, service/premium integration, schema 19 public transport and full-policy
acceptance remain pending under [the integration plan](../source-validated-energy-rules/current-integration-plan.md).
All six historical full-context examples failed grammar admission; some contain qualified or
differently expressed guarantees. No broad grammar coverage or producer readiness is claimed.
The normal extractor remains inactive and energy-only. Defaults remain V4/V19/V17/parser-v1 and
calculated-cost schema 18; beta and annual v2 are unchanged. No service parser activation occurred.

The 2602-test acceptance below covers only its older slice. Its pending user-decision statement
is superseded by the approval above. Older results are preserved, not extended to cover new work.
No deployment, reinterpretation or production action is authorized by local acceptance.

## Historical manager acceptance — bounded local scope only — 2026-09-15

**Accepted locally. Full policy is not complete. Nothing is deployed by this work.**
This section records acceptance of the earlier slice only. The manager reviewed the actual code,
effect cleanup and `isBilled` consistency, and the schema/prompt evidence gap.

Accepted scope includes shared lineage, six-month annualization, promotion guards, unchanged-energy
fee handling, current supplier/reset premium fallback and bounded mandatory base-effect Hybrid
projection. The final correction permits Hybrid FixedTerm contracts with active canonical resets,
not locked energy prices. Short Hybrid calls pass `resetPremium` and keep real-term clipping before
annualization. Supplier Hybrid remains OpenEnded-only. Locked prices, Historical isolation and
exact-period actual bills retain their safeguards. Schema 18, flags, beta and annual v2 are unchanged.

### Final evidence verified by the manager

Commands ran from `laravel/`. The full test used the isolated testing/SQLite environment recorded
in the correction section below.

- `php artisan test --no-ansi`: **2602 passed, 16393 assertions, 127.75 seconds**, exit 0.
  Log: `/tmp/voltikka-hybrid-term-full.log`.
- `npm run build`: **passed in 815 ms**. Log: `/tmp/voltikka-hybrid-build.log`.
  This build preceded the last PHP-only fix. No assets changed afterward.
- `vendor/bin/pint --test` over the union of changed tracked and untracked PHP paths:
  **66 files passed**.
- Task JSON, context pairs and `git diff --check`: **passed**.

These results supersede earlier final-gate counts and pending-manager status, not the recorded
facts of earlier runs. The subsequent status cleanup changes documentation and one calculator
comment only; it does not require another full suite or asset build.

Status-cleanup checks: `vendor/bin/pint --test
app/Services/CanonicalPricing/CanonicalContractPriceCalculator.php` passed from `laravel/`.
`git diff --check`, task JSON parsing, and root/CanonicalPricing `CLAUDE.md -> AGENTS.md`
symlink and byte-equality checks passed. The final diff and working-tree status were reviewed.
No additional full suite or build ran.

### Remaining decisions and release limits

- The user must decide explicit component price guarantees and fixed-versus-adjustable discount
  formulas before the larger energy-changing actual/normal plan. Phase dates and `normal_amount`
  alone do not prove a lock or an own-normal premium anchor. `phase-semantics.md` is a proposal,
  not approved new policy. Known promotion amounts and current fixed 12/24 policy are unchanged.
- Complete energy-promotion normal baselines, future energy spans/gaps and full-policy public copy
  and regression acceptance remain pending. Future work must reuse the existing billing timeline
  for full actual/normal plans per tariff bucket.
- The ignored local snapshot is too old for a current price-impact decision. Fresh-data impact
  and distinct-vintage EEX query-cost checks remain release prerequisites; no refresh is authorized.
- Acceptance is local only. No production access, deployment, application LLM calls, history rewrite,
  commit or push is authorized by it. Production actions require separate explicit approval.

## Historical run records — earlier status superseded

All sections below retain earlier run facts and status as history. Any statement that manager
acceptance is pending is superseded by the bounded local acceptance above. Earlier passing runs
do not establish full-policy acceptance or a release.

## Hybrid active-reset fixed-term correction — 2026-09-15

The manager's fixed-term review correction is complete locally. An active canonical recurring
reset, not the contract term label, determines whether Hybrid base energy can change. Current
reset frames now support the same OpenEnded/FixedTerm contexts for ordinary and explicit base-effect
pricing. Short Hybrid calls pass the selected reset premium and retain real-term clipping and
annualization. Supplier-adjusted Hybrid eligibility remains OpenEnded-only. Locked Fixed6/12/24,
Historical no-current-peer rules, schema 18, flags, beta and annual v2 remain unchanged.

General/Time/Season Fixed6 and Fixed12 tests compare separate ordinary/Hybrid peer families with
equal rates. Actual term, annualized/base and monthly costs agree; the known quarter stays exact;
no futures after the term are needed. Hybrid financial identity is recurring_forward_premium plus
BaseOnlyHybrid and excluded effect, not a numeric effect forecast. See `hybrid-projection.md`.

All commands ran from `laravel/` with:

```sh
APP_ENV=testing APP_CONFIG_CACHE=/tmp/voltikka-hybrid-no-config.php DB_URL='' DATABASE_URL='' DB_CONNECTION=sqlite DB_DATABASE=:memory:
```

- Pre-fix reproduction: `php artisan test --filter=test_fixed_term_hybrid_reset_matches --no-ansi`:
  **6 failed, 12 assertions** (`/tmp/hybrid-term-before.log`).
- Scoped: `php artisan test --filter='CurrentResetPremiumIntegrationTest|CurrentSupplierPremiumIntegrationTest|AsOfAnnualCostCalculatorTest|CurrentAsOfAnnualCostParityTest|CanonicalPricing\\' --no-ansi`:
  **373 passed, 5813 assertions, 25.43 seconds** (`/tmp/hybrid-term-scoped.log`).
- Full stable: `php artisan test --no-ansi`: **2602 passed, 16393 assertions, 127.75 seconds**,
  exit 0 (`/tmp/voltikka-hybrid-term-full.log`). This supersedes the manager's pre-correction
  **2596 passed, 16249 assertions, 129.13 seconds** at `/tmp/voltikka-hybrid-full.log`.
- `vendor/bin/pint --test app/Services/CanonicalPricing/CanonicalContractPriceCalculator.php tests/Feature/CurrentResetPremiumIntegrationTest.php`: passed.
- `git diff --check`: passed. Final diff and working-tree status reviewed.

The manager's pre-correction asset build passed in 815 ms. No assets changed and no new build ran.
Manager acceptance remains pending. This is not full-policy completion or deployment. Future energy
plans, real energy promotions and general gap policy remain pending. No production, network,
application LLM, migration command, history repair, commit or push occurred.

## Final phase/reset gate preparation — 2026-09-15

The focused corrections and stable local full suite are complete. **Manager acceptance remains
pending.** This is not full-policy completion or deployment. Root/context final status stays with
the manager. Schema remains 18; Historical policy and actual factual-period arithmetic stay unchanged.

### Changes

- `ContractDetailPageTest` now supplies the original June 30 FI Base Q3 reference at 50 EUR/MWh
  beside the July 24 curve. All popover assertions remain. Current no-today-reference behavior is
  unchanged. Pint changed equivalent imports only elsewhere in this test file.
- `candidateApplicablePhases` now proves expired absolute-end phases at their last covered past
  day and compares full energy maps. It does not skip unknown/different past energy or outside-window
  future plans. The date is candidate-only, never an energy observation or pricing anchor.
- Immutable current episode extraction uses shared full-map/current-coverage proof rather than a
  blanket rejection of later absolute fee starts. Actual trusted lineage anchors survive fee expiry.
- New General/Time/Season regressions cover real service forecasts, immutable predecessor evidence,
  Date/ContractStart fee introductions, stable full signatures, actual current fees, strict Historical
  rejection, byte-equal factual actual bills, unsafe old/future energy, and reset candidate parity.
  See `phase-invariance.md` and `reset-premium-integration.md` for scope and evidence.

### Commands and actual results

1. After initial test setup corrections, pre-fix `php artisan test --filter=test_absolute_fee_expiry
   --no-ansi`: **3 failed, 3 assertions**. Each July 16 candidate was null.
   Log: `/tmp/fee-expiry-before.log`.
2. `php artisan test --filter='test_absolute_fee_expiry|test_expired_phase_proof|test_reset_candidate_keeps_expired'
   --no-ansi`: **9 passed, 330 assertions, 1.87 seconds**.
   Log: `/tmp/fee-expiry-new.log`.
3. Scoped command from `laravel/`:
   ```sh
   php artisan test --filter='CanonicalPricing\\\\|CurrentResetPremiumIntegrationTest|CurrentSupplierPremiumIntegrationTest|CurrentPriceEpisode.*Test|CurrentEpisodePricingIntegrationTest|CurrentPromotionTermsTest|AsOfAnnualCostCalculatorTest|CurrentAsOfAnnualCostParityTest|BillComparisonCanonicalPricingTest|CanonicalOfferSurfacesTest|CanonicalPricingListingTest|ContractApiCanonicalPricingTest|ContractPricingReadModelTest|ContractCardPresenterTest|ContractDetailPresenterTest|ContractDetailPageTest|MarketResetEstimateSurfacesTest|EexMarketReferenceCurveProviderTest' --no-ansi
   ```
   **646 passed, 5737 assertions, 25.31 seconds**.
   Log: `/tmp/voltikka-phase-reset-final-scoped.log`.
4. Stable full command from `laravel/`:
   ```sh
   APP_ENV=testing APP_CONFIG_CACHE=/tmp/voltikka-phase-reset-no-config.php DB_URL='' DATABASE_URL='' DB_CONNECTION=sqlite DB_DATABASE=:memory: php artisan test --no-ansi > /tmp/voltikka-phase-reset-final.log 2>&1
   ```
   **2573 passed, 15019 assertions, 123.11 seconds; exit 0.** No PHP changed during this run.
5. Pint write and test on the five PHP files changed in this unit: **passed**. Only the detail test
   needed import formatting. Union of all changed and untracked PHP files: `vendor/bin/pint --test`
   with explicit paths: **64 files passed**.
6. `git diff --check`: **passed**. Final changed regions and working-tree status reviewed. Existing
   uncommitted work remains; context `CLAUDE.md` files remain symlinks.

No CSS/JS changed, so no new build ran. No network, production operation, application LLM,
migration command, dependency, commit, or push was used. Tests used isolated SQLite fixtures.
Fresh production data/performance validation and full-policy acceptance remain outside this unit.

### Immediately preceding manager run history (retained)

- Full suite: **2563 passed, 1 failed, 14685 assertions**. Failure was
  `ContractDetailPageTest::test_market_reset_contract_shows_the_current_price_and_estimate_qualifier`
  at line 1436. Its only curve was July 24; it lacked the original pre-July 1 reference.
- Asset build: **passed in 791 ms**, as reported by the manager.
- The new stable full result above supersedes the failed suite, not the recorded build result.


## Accepted implemented local slice — 2026-09-15

The implemented local slice passed final verification and has manager acceptance. The full approved policy remains incomplete and not deployed. Multi-phase, reset, Hybrid, and full-policy cross-path acceptance tasks remain pending.

### Superseding stable results

- Manager-run `cd laravel && php artisan test`: **2499 passed, 13080 assertions**, 296.60 seconds. Complete log: `/tmp/voltikka-annual-final-stable.log`; its final totals were checked during cleanup. This supersedes the prior full-suite failure retained below.
- Manager-run `cd laravel && npm run build`: **passed**, with a Browserslist database age warning only. The build and full suite were not repeated for the final style-only changes.
- `cd laravel && vendor/bin/pint app/Services/ContractPriceHistory/ContractHistoryPresenter.php app/Services/ContractStatistics/AsOfAnnualCostCalculator.php`: **fixed both files**. The before/after diff contains only equivalent imports and shortened type references (including PHPDoc). No functional change.
- With the local isolation prefix documented below, `cd laravel && php artisan test --filter='ContractHistoryPresenterTest|AsOfAnnualCostCalculatorTest|CurrentAsOfAnnualCostParityTest' --no-ansi`: **34 passed, 472 assertions**, 0.92 seconds. Log: `/tmp/voltikka-annual-final-style-focused.log`.
- All-changed PHP style check: collect the union of `git diff --name-only HEAD -z` and `git ls-files --others --exclude-standard -z`, select `laravel/*.php` paths including nested paths, then run `vendor/bin/pint --test` with those explicit paths from `laravel/`: **55 files passed**.
- `git diff --check`: **passed**. Final diff and status reviewed. `CanonicalPricing/CLAUDE.md` remains a symlink to `AGENTS.md`.

No approved policy, runtime behavior, or pending implementation scope changed in cleanup. No commit, push, network, deployment, or production verification occurred.

## Prior run history: six suite failures

### Prior result

The six assigned failures are corrected in tests only. All five assigned classes pass. The full suite has one failure outside this unit, in the premium-loader test owned by the manager. That test passes when run alone after the full run. The full suite is not verified as passing.

## Changes and reasons

- `laravel/tests/Unit/CanonicalPricing/VatIntegrationTest.php` (not Feature): the actual/normal VAT fixture now states a finite 12-month promotion. Its existing normal amounts supply the normal continuation. All financial expectations remain unchanged.
- `CanonicalOfferSurfacesTest.php`: the deliberately unresolved promotion now asserts `excluded_incomplete`, `insufficient_promotion_terms`, null annual cost, zero benefit, and no offer terms. Known complete offers remain in the company offer section.
- `CanonicalPricingListingTest.php`: the deliberately missing normal continuation is excluded from listing and ranking. The two complete contracts and the malformed-package exclusion remain tested. The test name and comment now state this purpose.
- `ContractPriceStatisticsCanonicalSourceTest.php`: the deliberate unknown normal continuation no longer creates a snapshot, even with a relational intro rate. The separate measured-offer fixture now has a 12-month boundary and retains its disclosed 7/9 c/kWh actual/normal prices.
- `WeeklyOffersCanonicalPricingTest.php`: the obsolete seven-query ceiling is replaced by an exact nine-query batch invariant: four contract/relation/Spot reads, four dated episode reads, and one available-futures-vintage read. The API has one more list read and therefore uses ten elsewhere; weekly offers uses nine. Data-provider cases prove the same query count for 1, 8, and 32 ordinary supplier candidates across three consumptions. Each case also proves exactly one futures-table read, no raw price-component reads, and unchanged offer membership and financial values.

No core code, guard, six-month arithmetic, historical method, root context, or task status was changed by this unit. Existing unrelated changes were preserved.

## Commands and results

Each test command ran from `laravel/` with this local isolation prefix:

```sh
APP_ENV=testing APP_CONFIG_CACHE=/tmp/voltikka-annual-verification-no-config.php DB_URL='' DATABASE_URL='' DB_CONNECTION=sqlite DB_DATABASE=:memory:
```

The PHPUnit configuration also forces SQLite `:memory:`, array cache/mail/session, test pricing defaults, and disabled Sentry delivery. No production, network tool, LLM, dependency, commit, or push operation was used. No CSS or JS changed.

1. Before edits:
   `php artisan test --filter='VatIntegrationTest|CanonicalOfferSurfacesTest|CanonicalPricingListingTest|ContractPriceStatisticsCanonicalSourceTest|WeeklyOffersCanonicalPricingTest' --no-ansi`
   Result: **30 passed, 6 failed, 194 assertions**, 1.17 seconds.
   Bounded log: `/tmp/voltikka-annual-targeted-before.log`.
2. After edits, same command:
   Result: **38 passed, 0 failed, 341 assertions**, 2.03 seconds. The provider adds two test cases.
   Bounded log: `/tmp/voltikka-annual-targeted-after.log`.
3. `php artisan test --no-ansi`
   Result: **2498 passed, 1 failed, 13078 assertions**, 296.62 seconds; exit status 1.
   Bounded final 220-line log: `/tmp/voltikka-annual-full.log`.
   Failure: `CurrentSupplierPremiumIntegrationTest::test_malformed_proven_canonical_peer_is_excluded_without_breaking_a_valid_alternative`, line 96. Expected `energy_general => 3.0`; actual `energy_general => 9.0`. No correction was made outside the owned files.
4. `php artisan test --filter='test_malformed_proven_canonical_peer_is_excluded_without_breaking_a_valid_alternative' --no-ansi`
   Result after the full run: **1 passed, 0 failed, 4 assertions**, 0.34 seconds.
   Bounded log: `/tmp/voltikka-annual-premium-failure.log`.
   This isolated pass does not establish a passing full suite or identify the cause of the earlier failure. The manager must reconcile the premium-loader review and full-suite result.
5. `vendor/bin/pint --test tests/Unit/CanonicalPricing/VatIntegrationTest.php tests/Feature/CanonicalOfferSurfacesTest.php tests/Feature/CanonicalPricingListingTest.php tests/Feature/ContractPriceStatisticsCanonicalSourceTest.php tests/Feature/WeeklyOffersCanonicalPricingTest.php`: passed.
6. `git diff --check`: passed. The final owned-file diff and `git status --short` were reviewed.
