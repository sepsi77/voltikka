# Decisions

## Renewed readiness boundary — 2026-09-16

Follow `../source-validated-energy-rules/release-readiness.md` for verified current evidence.
The manager accepts 27 independently checked numeric changes as policy-conformant, not empirically
better forecasts. The fresh candidate passes 375 full payloads in all four phases and lists 366;
only the new Kerava same-phase ambiguity exclusion differs financially from the independently
reviewed candidate. Current guards/short-duration/privacy/copy repairs preserve Historical.
Full synthetic video/template and UI job 228 pass within explicit limits; no actual posting is
approved. Final network-denied PHP job 235 passes (2812 tests / 21214 assertions / 106.56 seconds),
as do preflight/export smoke checks. The manager accepts default-V4 technical readiness for user
deployment approval. Job 237 and subsequent manager context/hash checks all passed; no technical
conditions remain. Exact results are in the child `release-readiness.md`.
No runtime blockers remain; full HTTP/live-cache/ranking behavior was not replayed.
V5 five-key activation, pilot/reinterpretation, producer accuracy and deployment remain separate,
unapproved work. No historical rebuild or active SQLite replacement. Earlier evidence is retained.

## Prior audit repair boundary — 2026-09-16

All child application repairs, export cleanup and bounded video repair/QA are complete. Final
manager gate passed: 2773 tests / 18253 assertions, Laravel build and Remotion lint/TypeScript/build.
The final sealed replay resolves broken anchors. See `../source-validated-energy-rules/audit-fixes.md`
for final replay files, limits and the completed report-only benefit cent comparison (14 smoke
cases; raw fields retained). Default-V4 release stays blocked for unaccepted material
pricing deltas and remaining audit decisions, not an unfixed NULL defect. No V5 accuracy, activation
or deployment approval follows. Historical identity/reference and Spot guard limits remain.

## Historical release boundary — FAILED production-data gate, 2026-09-16

The approved read-only preflight is complete. The staged default-V4 release is blocked by the
SQL NULL validation-success reader defect. See `../source-validated-energy-rules/production-preflight.md`
and its task list for local repair/regressions, repeat replay, query profiling and renewed acceptance.
No application repair or production mutation occurred. This is not only a V5 activation blocker.

## Historical release boundary — LOCAL PHP acceptance complete

Source-backed energy plans, financial normal-reference integration and public copy are implemented
locally. Shared calculated-cost schema is 19; defaults remain V4/v19/v17/parser-v1. The manager's
fresh combined full-policy PHP gate passed after schema 19: 2754 tests / 17909 assertions,
Laravel build and Pint on all 122 changed/untracked PHP files. Remotion ESLint, TypeScript and bundle
checks now pass after the approved install; visual verification remains pending, with no render done.
See `../source-validated-energy-rules/video-output-check.md` for logs and the earlier offline failure.
This is not production acceptance. Earlier bounded records below remain historical evidence. See `../source-validated-energy-rules/current-integration-plan.md` and
`../source-validated-energy-rules/release-plan.md` for current limits and separate deployment and
activation approvals. The fresh read-only production review is now complete but FAILED its release gate.


## Source-backed energy semantics approved — 2026-09-15

The user approved extending existing interpretation JSON with validated fixed-price periods and
discount formulas on adjustable tariffs, followed by current calculator and premium integration.
See [source-validated-energy-rules](../source-validated-energy-rules/spec.md). Its first unit isolates
versions only. Existing rows gain no new facts; unknown terms and Historical behavior stay unchanged.
The active v4/v19/v17 profile remains unchanged. V5 activation requires a separately approved release
and reinterpretation. This decision closes the pending semantic approval, not the pending full-policy
implementation or acceptance. No production action is approved.


## Foundation

- The approved policy is linked in `spec.md`; this task does not authorize release or production operations.
- Reuse the model's existing trusted predecessor resolution. Batch root/id pairs use recursive UNION, not depth-limited UNION ALL, so duplicate paths and cycles terminate by membership.
- Do not change matcher thresholds, graph writes, forward resolution, or pricing arithmetic in this unit.
- Keep the retail-premium single-root caller unchanged. The manager will integrate root and CanonicalPricing documentation.
- Later stages remain pending until their own implementation and verification are complete.

## Foundation result

- Shared model batch resolution is complete. Input is an array of string IDs; output is an ID-keyed array of `Illuminate\Support\Collection<int, string>`. Duplicate roots collapse; numeric string keys use PHP array rules. Membership order is unspecified.
- The history presenter no longer truncates at 25 predecessors. The former cutoff test now verifies all 28 versions and their price rows. Backward model chains exclude self even in cycles. These are intentional corrections; forward methods are unchanged.
- Exact retail-premium sorted membership and root hashes are tested for converging branches and cycles. No premium arithmetic changed.
- Final focused run: `cd laravel && php artisan test --filter='ElectricityContractLineagePriceHistoryTest|ContractHistoryPresenterTest|RetailPremiumCollectionTest|InferredRetailPremiumCollectionTest|RetailPremiumHistoryBackfillTest'` passed: 44 tests, 310 assertions. `git diff --check` passed.
- No production access, writes, release, commit or push occurred. Existing policy files were not edited. Root and CanonicalPricing context integration remained with the manager at this stage.

## Integrated local scope and pending acceptance (prior run history)

- Shared lineage, term/promotion guards (including explicit Spot-margin campaigns and expired evidence), dated full-bucket fee-independent episodes, and strict single-phase supplier premiums are implemented locally. This is not full-policy completion or deployment.
- Existing own reference remains first; the pure own-lineage/company/company-balanced market selector supplies the new current-curve fallback. Per-bucket offsets preserve the current month and fees. Method `supplier_adjusted_forward_premium` and basis `forward_premium` identify it. Historical replay never reads current peers; exact-period bills never receive annual projections.
- Pre-immutable Time/Season raw history needs same-date observed household identity/VAT proof. It cannot reopen invalid source-covered evidence. Resolver query counts are four General/five Time or Season; the General API is now ten with the curve-availability preflight. Earlier counts remain run history.
- Loader review removed the accidental same-day last-observed gate. Current pointed proof can remain current across midnight; first-observation/completion bounds, exact source proof, and no-lookahead remain. Provenance records actual last observation, bounded by as-of. A regression covers malformed proven canonical peers.
- Current shared calculated-cost schema is 18. No history rewrite, retained-row relabelling, or configured annual-method switch is part of this work.
- Pending units: multi-phase and fee-only eligibility; complete energy-promotion normal baselines and known future energy spans; reset-loader integration, including the old fallback-to-today path; Hybrid projections; full-policy public copy and regression acceptance.
- Final integrated verification is in progress until the manager confirms it. Earlier unit and failing full-suite reports remain dated run evidence, not final test totals. Documentation now distinguishes local completed slices from the unchanged approved target.
- This documentation unit changed no PHP or tests. No commit, push, production access, network operation, or history mutation occurred.

## Final accepted local slice — 2026-09-15

- Manager acceptance covers the implemented local slice only, not the full approved policy. All listed multi-phase, reset, Hybrid, and full-policy acceptance units remain pending.
- The stable manager-run full suite passed: 2499 tests, 13080 assertions, 296.60 seconds. The manager also reported a passing asset build, with only the Browserslist database age warning. These results supersede the prior failed run; that run remains in `final-verification.md` as history.
- Final cleanup only replaced fully qualified types with equivalent imports in the two assigned PHP files. No behavior changed. The three focused classes passed: 34 tests, 472 assertions. Pint passed for all 55 changed PHP files; `git diff --check` passed.
- The approved policy is unchanged. This acceptance does not claim deployment or production verification. No commit, push, network, or production operation was performed.

## Bounded unchanged-energy phase slice — 2026-09-15

- Complete: shared current extraction for ordinary unchanged General/Time/Season energy across redundant phases, equal normal metadata, and fully disclosed fee-only promotions. Original phases still supply actual and normal fees. Current month stays exact. Real energy promotions and future energy spans remain pending.
- The strict supplier eligibility primitive is unchanged. Both dedicated AsOf candidate preparation and calculator-internal preparation pass explicit Historical policy. Current dates do not imply Historical policy.
- A finite typed fee-only introduction may inherit its adjacent typed Normal energy baseline. An unidentified initial price or a genuinely Future energy phase cannot supply this proof. Existing source/publication/date/VAT and finite-promotion guards remain.
- Current annual normal billing no longer calls an ordinary fee change a promotion. Fee-phase estimates expose `monthly_fee_assumption=disclosed_phases`, with matching typed receipt text; plain estimates retain `held_flat`. Shared schema remains 18.
- The initial annual slice left exact-period normal fees unchanged and recorded false savings from a later ordinary fee increase. The authorized follow-up below corrects that bounded defect; this line records the initial finding.
- Phase-only fee introductions use their first normal continuation, not a more expensive fee disclosed later. Explicit normal amounts stay primary. Offer terms use that same bounded baseline.
- Scoped final verification: 471 tests, 4042 assertions, 9.56 seconds. Pint on all ten PHP files changed by this unit passed after formatting. Diff whitespace check passed. Full-suite and production-impact checks were not run. See `phase-invariance.md` for commands, interim failures, and the stale local-data limit.

## Authorized exact-period normal-fee correction — 2026-09-15

- Complete: annual and factual unchanged-energy bills share `unchangedEnergyNormalRates`. Ordinary fees stay on their own segments; explicit normal amounts remain primary; typed introductions use their first normal continuation. The current consumption-free candidate gates this period correction after the existing promotion guard.
- Actual period totals and phase breakdowns are identical to the original arithmetic. No annual energy forecast enters factual periods. Historical annual policy and candidate-ineligible energy-changing period rules are unchanged.
- General/Time/Season tests cover intro-only, both fee boundaries, and full-year periods, including Household/Company VAT, explicit normal overrides, and zero savings for ordinary increases. Factual fee savings retain days/30 proration, not annual contract-month proration.
- Final related scoped run: 474 passed, 4474 assertions, 14.79 seconds. Targeted integration: 49 passed, 1216 assertions. Pint on both changed PHP files and `git diff --check` passed. No full suite or production-impact check ran. Schema remains 18.

## Current reset missing-reference premium integration — 2026-09-15

- Complete locally; manager final gate remains pending. The calculator exposes a consumption-free typed reset frame and the existing request-local premium loader serves both families. Original own references remain first; Current never substitutes today's vintage for an unavailable old-period reference. Historical keeps that fallback and does not load current peers.
- The selector retains own-lineage, company, then company-balanced market priority with exact family, cadence, VAT and named tariff compatibility. Reset model dates use the pre-`min(period start, as-of)` vintage. Exact quarters and announced known periods are not forced into the supplier episode-month proxy. The selector independently checks observed dates and model bounds. Proxies retain explicit lower confidence.
- The new `recurring_forward_premium` method and `recurring_forward_premium_v1` policy use per-bucket offsets in actual, normal and structured bills. Exact tail starts protect known segments. Current six-month resets use only real-term tail months before annualization. Displayed equivalents use actual costed profile kWh. Exact-period actual bills, ordinary locked terms, schema 18, beta and configured annual v2 remain unchanged.
- The separate fee-helper correction was retained. This reset unit did not edit the fee helpers or their integration test. Real energy-promotion baselines, unknown gaps before future known prices, general future gap plans and Hybrid projections remain outside scope.
- Real SQLite source/publication and real-EEX repeated-period tests pass. Repeated identical period/vintage keys reuse queries; distinct model-reference dates can still add EEX SQL. A fresh-data release performance check is required, not a broad query refactor in this slice.
- Final scoped run: 537 passed, 5047 assertions, 19.20 seconds. Pint test on the 17 reset-unit PHP files and diff whitespace checks passed. No full suite, network, production operation, migration command, application LLM, history repair, commit or push ran. See `reset-premium-integration.md` for APIs, commands, interim failures, proof and remaining limits.

## Reset candidate temporal-proof review correction — 2026-09-15

- The manager's fee-only/partial-tariff future-borrowing defect was reproduced first. Six General/Time/Season candidate and direct-loader tests failed: global phase inheritance admitted an unsafe candidate and preferred its same-company premium over a valid market alternative. The Future phase components had the true `Future` price role.
- Supplier and reset candidates now share the supplier path's existing pure `candidateApplicablePhases` logic. Reset components and actual/normal rate maps use temporally applicable phases. A typed fee-only Introductory phase can still inherit its adjacent typed Normal baseline; current-structured fee phases, genuinely Future baselines and non-adjacent Normal phases cannot.
- This is candidate proof only. Existing billing inheritance, fee and offer helpers, reset calendars, estimator arithmetic and Historical routing are unchanged. Historical tests explicitly retain the old global-inheritance financial result; this does not claim to repair the broader old billing defect.
- Final new guard tests: 9 passed, 51 assertions. Scoped reset/phase/period/Historical/API/view run: 546 passed, 5098 assertions, 19.66 seconds. Pint test on the two changed PHP files and diff checks passed. No full suite or production/network/migration/history repair/commit/push operation ran. Manager final gate remains pending.

## Final focused fee-expiry correction — 2026-09-15

- An expired absolute-end phase can use its inclusive last past day for candidate-only coverage proof. The existing timeline/helper must resolve its complete energy map, and that map must equal every current map. Unknown or different old energy and outside-window future plans remain ineligible. This date is not a price observation or repricing anchor.
- Immutable current episodes now use this shared full-map/current-coverage proof instead of rejecting every later absolute phase start. Fee-only dated Normal boundaries retain the actual trusted lineage anchor. Future energy still cannot fill missing current rates. Dedicated Historical extraction remains strict.
- The reset detail popover test now supplies a legitimate June 30 FI Base Q3 original reference at 50 EUR/MWh beside its July 24 curve. All UI assertions and the no-today-reference rule remain. No estimator arithmetic changed.
- Nine new tariff regressions passed (330 assertions). The scoped phase/reset/period/Historical/view run passed 646 tests and 5737 assertions. Full-suite evidence and prior run history are in `final-verification.md`; manager acceptance remains pending. No broader approved-policy completion is claimed.

## Bounded current Hybrid base projection — 2026-09-15

- Complete locally; manager review remains pending. Current OpenEnded explicit base effects reuse
  the completed unchanged-energy supplier and known-period reset paths. Raw FixedPrice with those
  canonical facts normalizes to Hybrid. Complete structured/base proof remains required; effects
  are disclosure only. No exact status or quantified zero effect is fabricated.
- The one existing premium loader uses separate supplier/reset Hybrid-base family identities.
  Own references, trusted lineage/company/market order, cadence, VAT, actual dates and clone
  weights remain. Immutable current extraction shares the full-map proof; raw old snapshots do
  not gain Hybrid eligibility. Historical and exact-period actual bills remain unchanged.
- Primary methods identify the forecaster, while BaseOnlyHybrid/effect exclusion remains. A
  supplier hold-only result keeps HybridBaseOnly. Public copy states both estimated future base
  prices and the excluded effect. No public payload field, schema, flag, beta or annual v2 switch.
- Final scoped verification: 669 passed, 6967 assertions. Pint passed for 19 changed PHP files;
  diff whitespace check passed. No full suite or build ran. See `hybrid-projection.md` for exact
  commands, internal APIs, test proof, interim fixes and conservative source-evidence limits.
- Real energy-promotion baselines, future energy plans/gaps and full-policy copy acceptance stay
  pending. No network, production, application LLM, migration command, history repair, commit or
  push occurred.

## Manager correction: Hybrid fixed terms with active resets — 2026-09-15

- A fixed contract term does not lock energy under an explicit active canonical reset. Hybrid
  reset candidates now accept the existing ordinary FixedTerm contexts, and the short Hybrid
  call passes the selected premium. All known-base, cadence/family, date and real-term guards
  remain. Supplier projection stays OpenEnded-only; fully locked Hybrid6/12/24 stays unchanged.
- Six new General/Time/Season Fixed6/Fixed12 cases first failed, then passed with matching ordinary
  controls and separate compatible peers. They prove actual/annualized base costs, known quarter,
  premium method plus base-only identity, Historical isolation and no post-term futures requirement.
- Scoped: 373 passed, 5813 assertions. Full stable: 2602 passed, 16393 assertions, 127.75 seconds,
  exit 0 at `/tmp/voltikka-hybrid-term-full.log`. Pint and diff checks passed. Commands and prior
  manager full/build evidence are in `final-verification.md`. Manager acceptance remains pending;
  this is not full-policy completion. No production, network, commit or push occurred.
