# Annualized pricing implementation

## Renewed deployment readiness — 2026-09-16

Child runtime repairs, independent policy-conformity review, fresh strict-transport replay and
final UI checks are complete. The manager accepts the 27 numeric changes as policy-conformant,
not better forecasts, and accepts the full synthetic video/template within explicit limits.
Final network-denied PHP job 235 passes: 2812 tests / 21214 assertions; preflight/export smoke pass.
The manager accepts default-V4 technical readiness for user deployment approval. Job 237 and
subsequent manager context/hash checks all passed; no technical conditions remain. No runtime blockers remain.
See `../source-validated-energy-rules/release-readiness.md` for verified facts and limits; no full
HTTP/live-cache/ranking replay or V5 producer accuracy is claimed. No
commit/push/deploy, V5 activation, data mutation, historical rebuild or posting is approved.
Earlier results remain dated history, not verification of the final tree.

## Prior release status — 2026-09-16

All child application repairs, export cleanup and bounded video repair/QA are complete. Final
manager verification passed: 2773 tests / 18253 assertions, Laravel and Remotion checks. The final
sealed replay resolves broken anchors. The staged default-V4 release remains BLOCKED for economic
review of remaining premium/unknown-restart amounts and remaining policy/estimator disclosure
and guard decisions. The report-only benefit noise fix passes 14 smoke cases; raw fields stay intact. See
`../source-validated-energy-rules/audit-fixes.md` for results and bounded video limits. The original failed preflight remains
historical evidence, not release approval; V5 activation remains separate.

## Approved scope

Implement the full [approved CanonicalPricing policy](../../laravel/app/Services/CanonicalPricing/AGENTS.md#approved-annualized-comparison-policy-2026-09-15). The [policy decisions](../annualized-pricing-policy/decisions.md) retain the dated evidence and limits.

Stages:
1. Shared, batch-capable trusted replacement lineage for history and pricing identity. Preserve all stored history and forward redirects; do not add matching or graph writes.
2. Consistent six-month annualization, including Hybrid and disclosed in-term changes, and incomplete-promotion eligibility from structured and prose evidence.
3. Shared non-fixed-term forward estimates: known prices first, current FI futures for unknown months; premium evidence from own lineage, company, then market. Energy episodes compare full tariff rates independently of fees and ID changes. Preserve VAT, date-safe evidence, billing and cache boundaries.
4. Integration regressions and documentation for the complete policy, including equivalent phases, valid promotions, fallback evidence and accepted monthly projection estimates.

No new 24-month policy. The original implementation scope allowed no production access, release, mutations, commit or push.
A later separate approval allowed only the read-only preflight linked above; no production mutation occurred. No historical rewrite. Preserve the existing uncommitted policy documents. The manager owns root and CanonicalPricing context updates.

## Current local scope

The policy implementation and LOCAL PHP acceptance are complete within the source/evidence limits below, not deployed. Shared lineage,
term/promotion guards, dated full-tariff episodes, and the strict single-phase supplier-premium
slice are implemented locally. Ordinary unchanged-energy General/Time/Season tariffs now also
share redundant-phase and fully disclosed fee-only forecast eligibility, billing, peer evidence,
and immutable energy episodes. Annual and factual-period normal fees also share the first-normal
baseline; ordinary future fee increases do not create period savings. Actual period bills and
Historical annual policy are unchanged. Expired absolute-end fee phases now retain eligibility
only after complete past/current energy equality proof. They do not create repricing dates, admit
changed old energy, or skip outside-window future declarations. See `phase-invariance.md` for the
bounded slice and checks.
Current reset missing-reference premiums now use the same batched evidence loader, with per-bucket
adjustments and real-term horizons. Current no longer uses today's old-period vintage; Historical
retains it. See `reset-premium-integration.md` for local tests and `final-verification.md` for bounded acceptance.
Current explicit base-effect Hybrid projections now reuse unchanged-energy supplier and known-period
reset paths, with separate compatible premium families and composed public disclosures. See
`hybrid-projection.md` for the bounded local proof; bounded manager acceptance is recorded.
Source-backed energy-promotion normal baselines, known future energy spans/gaps and strict public
copy are now implemented locally. See `../source-validated-energy-rules/current-integration-plan.md`.
The final manager full PHP gate passed after schema 19; see the child `final-verification.md`.
Remotion ESLint, TypeScript and bundle checks pass. Full synthetic 750-frame video and phone-size
browser playback checks pass; real-feed, physical-phone and social-compression coverage is not
claimed. See child `release-readiness.md` and `video-release-fixes.md`; earlier failures remain history. Local acceptance is not production acceptance or producer activation. Shared calculated-cost schema is 19; interpretation defaults
stay V4. See the child `release-plan.md` for separate deployment/activation approvals.
Earlier bounded gate and unit counts remain historical evidence in `final-verification.md` and the
unit reports; the child current acceptance section records the new combined gate.

## Initial foundation unit

Add `ElectricityContract::getReplacementLineageIdsByContractIds(array $contractIds): array`, with Collections of string IDs per requested root. Use one parameterized recursive UNION query; include existing roots and return empty Collections for missing roots. Share it with the single-root wrapper, backward models and history presenter. Test isolated IDs, branches, chains, cycles, missing roots, mixed batches and query count. Preserve retail-premium identity and existing history output.
