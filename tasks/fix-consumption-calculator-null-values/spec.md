# Fix consumption calculator null values

Prevent 500 errors when Livewire numeric/select inputs are cleared and blurred. Add safe defaults/validation so calculations can proceed or recover gracefully.
