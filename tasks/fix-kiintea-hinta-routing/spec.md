# Fix kiinteä hinta routing

Restore `/sahkosopimus/kiintea-hinta` as an indexable fixed-price contract listing page, and stop non-municipality slugs from rendering fake `/sahkosopimus/paikkakunnat/{location}` pages.
