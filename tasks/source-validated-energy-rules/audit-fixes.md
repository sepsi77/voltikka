# Local audit repairs — current consolidated status, 2026-09-16

## Renewed readiness gate — default-V4 technically ready for user deployment approval

`release-readiness.md` now records the fresh verified export/hash, strict public transport in all
four phases, independent review of all 27 numeric changes and the additional Kerava exclusion.
The manager accepts policy conformity, not empirical forecast improvement. Current estimator,
privacy, short-duration, copy and asset repairs are complete. Full synthetic video/template review
and UI job 228 pass within explicit limits. Final network-denied job 235 passed: 2812 tests /
21214 assertions / 106.56 seconds; preflight/export smoke also pass. The manager accepts default-V4
technical readiness. Job 237 and subsequent manager context/hash checks all passed; exact results
are in `release-readiness.md`. No technical conditions remain.
No runtime blockers remain, but user deployment approval is still required. No deployment,
posting or V5 activation is approved; active production has not received these fixes. The sections below retain
superseded dated results; their pending amount/video statements are not the current status.

## Prior release status

**Default-V4 release remains BLOCKED.** The NULL-reader defect is repaired locally. The intermediate replay is not accepted: subsequent review proved two further anchor defects, now repaired with local regressions. All application units are complete and the manager reviewed actual source changes. Final full-suite/build verification and the fresh same-sealed-database replay are complete. Broken anchors are resolved. Remaining premium and unknown-restart amounts are not accepted as improvements. Economic review and remaining audit decisions are required; the original NULL bug is not the remaining blocker. Nothing in this report authorizes deployment, producer activation, reinterpretation, commit or push. Local calculated-cost schema is 19; the deployed baseline is 17. Interpretation defaults remain V4/v19/v17/parser-v1; validator-v18, source-schema-v5 and annual-v2 retain their distinct identities. No V5 data was present in the replay. Producer accuracy is unmeasured.

This report supersedes earlier current-status paragraphs about pending repair, replay and query diagnosis. Original failed preflight results and dated unit results remain factual history. Read the per-unit notes for exact implementation and commands.

## Completed local repairs

- `nullable-anchor-repair.md`: SQL NULL, JSON null and [] mean successful validation. Malformed JSON, nonempty lists, scalars and objects still fail closed. A real queued job with fake HTTP proves writer-to-reader compatibility. Focused tests: 41 / 336 assertions; related tests: 311 / 5174 assertions. The manager reviewed code and tests.
- `public-audit-repairs.md`: premium-reset copy describes its actual basis. Real short-term metadata drives SEO, FAQ, footer, weekly and type-comparison output, including Hybrid. Copy does not claim unknown continuation unconditionally. A quiet term fact does not consume the two-warning limit. About-page methodology includes the premium rung and estimated normal benefits.
- `estimate-disclosure-repair.md`: the nested normal Supplier fee assumption is `disclosed_phases` if either actual or normal fees vary within the real calculation window. Two different but constant fees remain `held_flat`. Estimated negative normal/direct-adjustable clamps now set the model-clamp flag. Financial arithmetic and source floors do not change. `actual_projection` remains null when no actual estimator object exists; no provenance is fabricated.
- Documentation consolidation corrects current schema references and reader status, the retracted beta comment, Historical scope exceptions and the reset-only enable flag. No configuration value changes.

## Earlier combined verification supplied by the manager

- Laravel `php artisan test`: **2766 tests, 18151 assertions, 144.10 seconds**, passed. Log: `/tmp/voltikka-audit-fixes-full.log`.
- Laravel `npm run build`: exit 0. Log: `/tmp/voltikka-audit-fixes-build.log`.
- Remotion `npm run lint` (including TypeScript) and `npm run build`: exit 0. Logs: `/tmp/voltikka-audit-fixes-remotion-lint.log` and `/tmp/voltikka-audit-fixes-remotion-build.log`.

These are manager-run checks, not runs by the documentation executor. They precede the final anchor, short-term qualifier and video fixes. Those units now have passing focused verification below. The final combined gate supersedes these earlier counts, without rewriting their historical result. Earlier offline dependency failures and later successful locked dependency/lint/build checks remain in `video-output-check.md` and the parent verification history.

## Historical manager combined gate and sealed replay — superseded by fresh release check

- `php artisan test`: **2773 tests / 18253 assertions / 138.45 seconds**, passed.
- Laravel `npm run build` and latest Remotion `npm run lint` (including TypeScript) / `npm run build`: all passed.
- Logs: `/tmp/voltikka-audit-final-{full,build,remotion-lint,remotion-build}.log`.
- Final candidate: `/tmp/voltikka-energy-preflight-20260916-a/audit-fixes-final-candidate.json`. Use the corrected report `/tmp/voltikka-energy-preflight-20260916-a/audit-fixes-reviewed-comparison.json`. The earlier `audit-fixes-final-comparison.json` remains unchanged historical evidence. Original sealed database hash preserved; **375 contracts at each consumption**, cold/warm business rows identical. **367 listed; 28 changed displayed states (27 numeric plus Hehku)** at all three consumptions.
- All eight Time/Season anchors are January 21; both Fortum anchors are June 15 and their totals now exactly match the deployed baseline; Parikkala `gduez1` starts September 10. The three Lammaisten July 24 restarts remain: the July 23 unknown interpretation correctly breaks continuity.
- Maximum absolute annual deltas: **509.406842 EUR at 5000**, **203.762737 EUR at 2000**, **1833.864631 EUR at 18000 kWh**. Remaining premium and unknown-restart amounts are not automatically improvements and have not received economic acceptance.
- Original un-ANALYZED snapshot: cold **52.0446 s / 33 SQL / 48.4011 s SQL**; warm **1.9327 s / 1 SQL / 2.97 ms SQL**. This is local SQLite evidence, not production latency.
- Report-only noise correction is complete and manager-reviewed. `displayedBenefitAmount` rounds EUR to cents while preserving null versus zero, eligibility and raw diagnostic fields. **14 smoke cases passed.** The corrected report has **28 displayed-state changes and 2 benefit transport changes at all consumptions**: Vaasa annual-equivalent **5.90 → 11.80**, with real-term **5.90 unchanged**; Hehku **4.90 → unavailable**. Aalto **5.95 stays unchanged**. SUPERDIILI remains listed with **0.32 margin and 17.80 benefit**. The same final candidate was reused; original reports are unchanged. No financial calculation changed.
- Manager Pint passed on **all 131 changed/untracked Laravel PHP files**; log `/tmp/pi-bg/job-15523-182.log`. The final 2773-test gate remains valid because the last change was report-only.

These are manager-supplied results, not documentation-executor runs. All application units are complete; this is not all-audit completion or release approval.

## Earlier intermediate sealed three-consumption replay

The completed replay uses the original unchanged database hash, all 375 contracts, and comparison date 2026-09-16. It covers 5000 kWh cold/warm and 2000/18000 kWh. Cold and warm business rows match. Evidence:

- `/tmp/voltikka-energy-preflight-20260916-a/audit-fixes-candidate.json`
- `/tmp/voltikka-energy-preflight-20260916-a/audit-fixes-comparison.json`

All 46 lost forecasts return: 24 old forward → forward; 12 old seasonal → forward; 8 old seasonal → premium; 2 old forward → premium. One additional old exact result becomes premium. At each consumption, 30 displayed states change, including one new Hehku exclusion; there are 29 paired numeric deltas. Listed membership changes from 368 to 367.

**Historical intermediate results only:** superseded by the final repaired-anchor replay above.

Maximum absolute annual deltas: **625.203113 EUR at 5000**, **250.081245 EUR at 2000**, and **2250.731206 EUR at 18000 kWh**. These are not automatically improvements. The completed first anchor review found defects; final amount review must follow their repair and a new replay.

On the original un-ANALYZED SQLite snapshot, cold time is 52.914 seconds with 32 SQL queries / 49.257 seconds SQL; warm time is 1.993 seconds with one query / 2.93 ms SQL. These are local replay measurements, not production latency.

## Query diagnosis and export work

`query-profile.md` proves that the identical query improves from 25.348 seconds to 0.125 seconds after `ANALYZE`. All 12 business-table row digests and the 6552-row result multiset match. This is a SQLite optimizer artifact, not evidence for an application query change or production MySQL latency. The corrected NULL diagnostic has zero actual mismatches and 950 legacy NULL pointers.

Export cleanup is complete: original NONUNIQUE lookup indexes are restored and `ANALYZE` is added; unnecessary unique-index preservation is removed. Uniqueness alone did not repair the query plan. The manager reviewed the helper and ran network-denied `smoke` and `export-smoke`: both PASS, including six bad manifests, twelve bad index cases and explicit primary-key, graph and NULL checks. This local export verification is not a new production export or a production latency measurement.

## Further anchor review — repairs and final replay complete

The manager reviewed actual code and inputs; no new evidence policy was introduced. Review classifies the 30 changed states as:

- **11 definite anchor defects:** ten Time/Season histories reject valid `has_discount=0`, `discount_type=NoDiscount` and zero discount fields. Eight anchors incorrectly start July 23 instead of January 21; two Fortum anchors should start June 15. Parikkala `gduez1` has ancestor 7.85 ending September 9 and current 12.92 continuing to September 16; a missing end-plus-one event incorrectly starts the current run September 16 instead of September 10.
- **8 supported changes:** three Lammaisten histories correctly break at the missing July 23 interpretation, three lineage/date corrections, Fixed6 and Hehku.
- **11 premium routes approved in principle:** their amounts required a new replay after donor-anchor repair. That replay is now complete; economic acceptance is still pending.

`anchor-boundary-repairs.md` records both completed repairs. Strict inactive `NoDiscount`/NULL zero numeric metadata is accepted; active, unknown and malformed data stays rejected. Covered end-plus-one events remove expired conflicts without inventing observations. Source proof, VAT, full tariffs, normal-mode raw-history prohibition and gap policy are unchanged. Lammaisten July 23 unknown evidence still breaks continuity. Focused tests: **46 / 412 assertions**; related tests: **316 / 5252 assertions**. The manager reviewed source changes. The existing sealed audit-fixes replay remains intermediate, not final pricing acceptance.

## Final public and bounded video repairs

- `public-audit-repairs.md` appendix: a real V4 six-month 6→8 c/kWh fixture reached the incorrect constant-price qualifier. The short-term branch now neutrally describes published term prices, with annualization added only when no estimate explainer exists. No new price guarantee or financial change. **183 tests / 788 assertions** passed; the manager reviewed code.
- `video-visual-review.md` appendix: Promo/SignOff no longer force complete thumbnail state at Sequence-local frame zero. Canonical `base_only_hybrid` visibly says `Ei sisällä kulutusvaikutusta`. Lint/build, private harness TypeScript, network-denied rendering and checks on **12 PNGs** passed. The manager inspected final Hybrid frame 385 and the TypeScript diff. Evidence: `/tmp/voltikka-weekly-visual-qa-after/`.
- The final synthetic Hybrid fixture uses authoritative `base_only_hybrid`, unlike the initial `comparable` fixture. Tested font fit, labels and sampled scene boundaries pass. This is not real-financial-feed verification. Phone reading time, platform overlays/compression, full video/audio and complete frame-by-frame coverage remain outside this bounded QA. The bounded visual task is complete, not general release approval.

## Open gates and policy decisions

- Review remaining premium and unknown-restart amounts economically before renewed default-V4 release acceptance. Final tests and replay pass mechanically, not as financial approval.
- Keep broader video/real-feed limitations explicit when deciding release scope. The bounded visual fix and check are complete.
- Supplier/Reset hold-beta, floor, vintage metadata and guard review remain open. SourceEnergyRule floor flags are fixed; this is not a claim that all estimator disclosures are fixed.
- Seasonal anchor weight policy remains unresolved. Nonexact duration buckets must not invent exact months. Fee-only short-term future-baseline inheritance requires in-term proof, not blind borrowing. This batch does not silently resolve these decisions.
- Historical deliberately keeps narrower identity/reference behavior; shared current lineage or numerical settings do not imply identical Historical behavior. `RESET_FORWARD_SHIFT_ENABLED` controls only the reset path, not all estimators. Spot negative-wholesale and guard policy are unchanged.
- V5 pilot, producer accuracy, activation and reinterpretation remain separate, unapproved gates.

## Documentation-only verification

The documentation executor ran `vendor/bin/pint --test config/canonical_pricing.php`: passed.
A PHP token comparison against HEAD confirmed that all non-comment tokens, including every
configuration value, are unchanged. `git diff --check` passed. Python JSON parsing/schema checks
passed for both task lists. Byte comparisons passed for all twelve changed AGENTS/CLAUDE pairs,
including Livewire, ContractCard, ContractDetail and Remotion; all twelve CLAUDE files remain
symlinks. The final manager log tails were also inspected: 2773 tests / 18253 assertions,
138.45 seconds; Laravel build 823 ms; Remotion lint/TypeScript and bundle completed. Final diff and worktree status were reviewed. No application
code changed in this unit except comments in the config file. No application tests were rerun
for this documentation-only unit; the combined application evidence above is manager-supplied.
